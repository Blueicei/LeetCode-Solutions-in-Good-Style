```
Todo List
巩固：算法部分，操作系统，IO多路复用/零拷贝/socket—RPC，并发编程，JVM
项目：citylib迁移业务，AOP redis缓存/日志，异步消息队列kafka/rocketMQ，分布式两台主机，docker k8s容器，压力测试qps。高并发项目/手写RPC框架
下一步：沿着hadoop全家桶，继续探索
source /home/fuke/anaconda3/bin/activate mamba_env
python3 -u train.py --gpu 5 --dataset 7thousand --gama 0.1 --mask_ratio 0.2 --trans-hidden-dim 32 --trans-feedforward-dim 128  --validate-epoch 50 --epochs 50 --rnn transformer --save 7thousand_trans_hid32_long_mlm_gama01_maskraito02_con1 --batch-size 256

python -u train.py --gpu 0 --dataset 7thousand --gama 0.1 --mask_ratio 0.2 --trans-hidden-dim 32 --trans-feedforward-dim 128  --validate-epoch 50 --epochs 50 --rnn transformer --save 7thousand_trans_hid32_long_mlm_gama01_maskraito02_con1 --batch-size 256

基于

智慧城市交通管理系统设计与实现

基于深度学习的人类活动预测系统设计与实现

城市环境下的移动数据分析与行为建模研究

城市环境下的人类活动数据分析与系统研究

1.基于深度学习的智慧城市交通管理系统设计与实现
2.城市环境下的人类活动数据分析与可视化系统实现
3.城市居民移动行为的分析、预测及可视化系统实现
4.基于深度学习的城市居民移动行为预测与可视化系统实现
5.基于深度学习的城市居民移动行为预测系统设计与实现*
6.基于深度学习的城市居民移动推荐系统设计与实现*
7.基于人类活动数据的轨迹预测系统设计与实现*
8.城市居民移动行为预测：基于深度学习的数据驱动方法与可视化分析系统设计
基于人类活动数据的轨迹预测系统设计与实现
基于人类活动数据的轨迹预测系统设计与实现

基于兴趣点推荐的城市交通管理系统设计与实现

基于兴趣点推荐的城市居民活动分析系统设计与实现
基于位置预测的城市居民活动分析系统设计与实现

基于多源异质数据的中药用药风险预警研究及系统

基于轨迹分析的城市居民

基于用户轨迹的城市移动行为分析系统
基于用户轨迹的城市移动行为分析系统


1.基于用户轨迹的智慧城市分析系统
2.基于用户轨迹的城市潜在活动规律分析系统
3.基于用户轨迹的城市居民活动分析系统
4.基于用户轨迹的城市居民活动规律挖掘系统
5.基于用户轨迹的城市居民生活空间感知系统

基于用户轨迹的城市活动空间感知系统

基于用户轨迹的城市空间数据挖掘系统

这里不应该是空间，应该是活动/移动，体现转移过程中提取出的规律

基于用户轨迹的城市时空数据挖掘系统
基于用户轨迹的城市时空数据分析系统
基于出租车GPS数据的居民出行行为分析

基于轨迹数据的城市居民出行热点与出行模式研究
基于多模态轨迹数据的城市空间感知研究
基于多源轨迹数据挖掘的居民通勤行为与城市职住空间特征研究
时空轨迹数据挖掘及其在城市居民出行行为模式分析中的应用研究——以北京市居民出行轨迹数据为例

城市居民生活方式分析
城市空间流动
城市流动性分析系统
城市脉动分析系统
城市集体运动分析系统
群体轨迹分析
群体流动/转移分析

收集到的硕士毕业论文题目：
基于出租车数据的城市居民活动空间与网络时空特性研究[D]. 武汉大学, 2016.
基于轨迹数据的城市居民出行热点与出行模式研究[D]. 江西理工大学, 2021.
时空轨迹数据挖掘及其在城市居民出行行为模式分析中的应用研究[D]. 安徽师范大学, 2019.
城市居民出行模式多维度分析与挖掘方法研究[D].中国地质大学(北京),2021.
基于大数据的城市居民出行分析建模[D]. 浙江大学, 2018.
基于轨迹数据的城市居民出行模式分析与挖掘[D]. 大连理工大学, 2019.
基于多源时空轨迹数据聚类的居民出行活动模式挖掘[D]. 深圳大学, 2019.
基于出租车数据的城市居民出行时空特征分析与空间结构信息挖掘[D]. 青岛理工大学, 2022.
基于出租车轨迹数据的居民出行特征及影响因素研究[D]. 重庆交通大学, 2022.
基于出租车轨迹数据的城市居民出行时空特征挖掘与可视化分析[D]. 长安大学, 2021.
基于位置服务数据的城市活动空间研究[D]. 南京大学, 2013.
基于居民行为的兰州市西固区城市活动空间研究[D]. 兰州交通大学, 2023.
基于手机信令数据的城市人群活动时空格局分析研究[D]. 南京师范大学, 2021.
基于居民时空间行为特征的城市活动空间研究[D]. 天津大学, 2019.
总结：
基于xx数据的城市居民出行xx
基于xx数据的城市活动空间xx
没有找到：城市居民活动xx


GPT生成的（城市居民活动<粒度<城市时空数据挖掘/分析）：
城市空间流动分析系统
城市流动性分析系统
城市脉动分析系统
城市集体运动分析系统
城市群体轨迹分析系统
城市群体流动分析系统
城市群体转移分析系统

之前的：
基于用户轨迹的城市居民活动分析系统

我自己想的：
基于轨迹数据的城市居民出行模式分析系统
基于用户轨迹的城市居民活动规律及出行时空特征分析系统
基于用户轨迹的城市活动空间分析系统


研究背景写的不太行，制作ppt
可能要合并卡口和出租车轨迹分析
重新做综述和引用

截至2022年全国城镇人口占比65.22%,
受经济回升向好、人口迁移流动更加活跃等因素影响，城镇人口继续增加。2023年末，我国城镇常住人口达93267万人，比2022年增加1196万人。
党的十八大以来，城市规模结构持续改善。2021年末，全国城市数量达691个，比2012年末增加34个。
按2020年末户籍人口规模划分，100万—200万、200万—400万、400万以上人口的地级以上城市分别有96个、46个和22个

随着经济建设的飞速发展，国家工业化和城市化程度迅速提高，城市人口及空间规模不断扩大。据统计，2022年全国城市城区面积为191,217平方公里，人口超过100万的城市已达167个。
受经济回升向好、人口迁移流动更加活跃等因素影响，城镇人口继续增加。2023年末，我国城镇常住人口达93267万人，比2022年增加1196万人，占比全国人口的66.16%。
受经济回升向好、人口迁移流动更加活跃等因素影响，城镇人口继续增加。截至2022年，我国城镇常住人口达92071万人，比2021年增加646万人，占比全国人口的65.22%。

[1]国家统计局. 中国统计年鉴[M]. 中国统计出版社, 2023.
王萍萍. 人口总量有所下降，人口高质量发展取得成效. 中国统计出版社, 2024.

[1]Gang Pan,Guande Qi,Zhaohui Wu, et al.Land-Use Classification Using Taxi GPS Traces[J]
Daqing Zhang & Shijian Li.(2013).Land-Use Classification Using Taxi GPS Traces.IEEE Transactions on Intelligent Transportation Systems(1),113-123.

此外，研究轨迹数据挖掘相关技术也具有广泛的应用价值。例如，对人类轨迹数据的挖掘能够更好的理解人类行为动力学特征，分析用户的出行行为习惯 [5]，预测用户位置[6]，为用户推荐兴趣位置[7]。同时，对人类轨迹数据挖掘有利于探索理解人类社会活动特性，如个人活动行为的出行偏好挖掘、群组模式挖掘、整个城市的人口的移动模式挖掘，从不同粒度、不同层面挖掘探索人类出行行为对社交活动的影响。对城市交通的轨迹数据的挖掘能够为交通路线优化 [8]、城市的人口流动特点研究 [9]，防范公共安全事件 [10]，城市功能区域规划 [11]，个性化推荐路线、路网预测、城市规划等提供很好的解决方案[12]；

核心功能：
1.基于交通摄像头和出租车GPS记录的车辆轨迹分析模块。不仅包括基本的车辆轨迹管理功能（如轨迹的查询，可视化），还提供多维度的统计、分析功能，有助于城市规划者更深层地理解城市居民的具体活动规律和特征。
2.出租车乘客上下车热点分析模块。通过位置聚合、热点区域划分和按热度分级等方法进一步分析、可视化城市热点分布情况，并且发现不同层级热点之间存在伴随和抑制两种模式。对于城市规划者进一步理解城市空间分布有很大帮助，帮助城市规划者进行商业选址、广告投放等决策的制定。
3.基于交通摄像头的城市流量与流向分析模块。该模块是从交通摄像头的角度，分析不同时间下道路路口流量分布情况和整体城市居民流向情况。帮助城市规划者从全局的视角，俯瞰城市居民整体移动趋势和活动规律，可以应用于解决交通拥堵、公共设施建设等决策过程。
4.位置预测。根据上述分布模块所得出的城市居民活动规律，结合当前用户的历史轨迹，预测用户下一个要访问的位置，位置预测在各种应用中发挥着关键作用，如基于位置的推荐、交通优化和流行病控制。

数据挖掘太像算法了，不像系统
大的系统分成好几个模块
2个算法+一个系统
已修22学分
专业实践8分，海外1分
这周重点，看论文，写综述，把要做的东西整明白，正反馈/工作量。整体的框架图。八股/算法/投简历
看一篇写相关综述。
1.轨迹数据挖掘研究现状
2.城市居民活动特征分析研究现状
把以前综述第一段改为绪论
服务于城市规划者的城市居民活动分析系统：决策服务于个人/提升具体的生活幸福感，也服务于整体/具有长远累积效益
背景/意义 加点图：一张大图包括：轨迹数据一张图+居民活动与城市关系一张图+具体决策应用一张图（个人/整体）
整理城市居民出行活动方面的学位论文，为完善表述毕设绪论部分做准备
回顾之前看过的位置预测相关的论文，整理记录下可能有用的思路，先一步步积累一下，准备好深入扎进某一个点，再coding。
看到的是一个有意思的叫：短期依赖下，连续轨迹中的非连续但是空间/时间周期相近的点才是预测下一位置的关键，并不一定是按序列中的先后位置关系决定的。进一步引申出关注模型的上下文推理能力，不过这里的上下文和序列顺序无关，而是需要引入轨迹点的时空属性作为时空上下文。

位置预测基于基本的用户序列，一般应用广泛的nlp序列模型（lstm/transformer/mamba），用户轨迹区别于文本，稀疏/缺失/不具备语义，极大依赖于用户偏好，缺少domain knowledge/和城市时空关联性不大，个体与整体的关联性几乎忽略不计，但其他用户会contribute当前用户预测，因此个体to整体粒度更细一点，个体toNeighbor，聚合方式希望是自学习/自监督loss/具备轨迹-目的-当前位置的用户识别并contribute当前用户和位置的预测/序列模型输入不带user从输出开始执行/希望能记录下其他用户的历史状态提供搜索/mamba单用户对所有用户的选择遗忘-因为构建不出确定性的图和边，lda类别有上限因为是基于word而不是sentence。同时用户偏好决定了，user-document相区别，user影响更大/user间关联性更强，靠用户偏好聚合特定用户/特定目的的neighbor
更新用户neighbor图，不限时间，对比学习随时更新正负用户样本/test的时候如何只聚合正样本，每个用户每个位置的context构造负样本，训练用户/位置编码器+残差连接。从动量中获取正样本
正样本来自时间，序列mask区别。负样本和其他用户

用户个体以上的粒度中，什么信息能对该用户的位置预测产生帮助。除了用户个人偏好和周期性规律外，他人/整体的轨迹序列、location的经纬度/category、用户社交网络能对个人预测产生多少帮助。
1.基于图的location embdding，基于lda的user embedding。都是来自整体轨迹序列中相邻位置的关系，来自整体的粒度太粗了，对于个人偏好占极大比重的轨迹预测中效果并不明显，而且大多数时刻并不是稳定的，带有强烈的用户个人主观意愿。更细粒度一点，局部轨迹/用户，存在搜索复杂度高/稀疏/动态变化的问题，如何筛选，根据相同的用户出行目的推断中间的连续位置，目的如何推断/既然能推断出目的直接回顾历史轨迹不更好吗
2.location的粒度太细了，比如家可以分布在城市任何位置，学校分布在各处，写字楼/商业聚合体到处都是，起点相同终点往往是截然不同。单纯按轨迹/category聚合分类，只能提供一个很大候选集，在用户偏好面前同类别差异性也很大。比如：个人真正的规律性生活来自两点/三点一线，家-学校/单位/商业聚合体-家，从整体的视角来看任意位置出发都无法预测下一位置，从局部的视角来看能提供帮助来源的位置/用户是动态便变化的且稀疏。只有特殊情况才具备整体流动特性，比如某个旅游区域的旅游规划路线，趵突泉30min->宽厚里30min->黑虎泉...。卡口的粒度刚刚好，可以定义一个独特的区域功能

学长底板(居民活动)->和个体/城市的关系->应用,city-foundation model3层的图

  map = new AMap.Map('container', {
    resizeEnable: true,
    rotateEnable:true,
    pitchEnable:true,
    zoom: 17,
    pitch:80,
    rotation:-15,
    showLabel: false,
    mapStyle: "amap://styles/grey",
    viewMode:'3D',//开启3D视图,默认为关闭
    buildingAnimation:true,//楼块出现是否带动画
    
    expandZoomRange:true,
    zooms:[3,20],
    center:[117.14,36.67]
  });
画一个知识图谱
基于路网citespace。扩展大模型（单纯对话）。基于POI。基于region
先只跑一下CLLP的对比学习部分，测试下效果
对比学习很大程度上是解决数据稀疏性和冷启动的，我想的是向量空间区别度粒度更细一点。
首先要确定正确的数据增强方法，后面才能少走弯路不受前面累积错误的影响
两种粗细粒度，联合增强，8:2，粗（替换-batch），细（rank3：10，看一下分布情况），不够就用reorder替换（3个对10个）。变数太多，工作量太大了
组会提到从另一个角度完善encoder的表达能力，传统基于单个location prediction loss的encoder训练模型，忽略了未来序列在当前位置预测上的贡献，因该训练时主动用multi-task的方式同步完善该模型。
探索一下自监督，增强上下文感知能力的通用loss和方法

回顾linyan的ctle那篇论文《Pre-training Context and Time Aware Location Embeddings from Spatial-Temporal Trajectories for User Next Location Prediction》，他也是用mlm去预训练整个序列编码器，除了token还有time_slot的mlm loss。motivation中提到用mlm学习序列上下文，是为了学习具有多功能的poi在不同上下文中，展示出其特定的功能，方便预测阶段根据上文做辨别，按道理mask token的选取是否应该要侧重于这种高频上下文不一致的location_id。
但是没有提供数据和代码，在消融实验中也是把已经包含了location mlm的transformer作为了base，只展示了添加time_slot mlm后有提升，因此也看不出具体location mlm有没有作用。论文中提到关于mlm mask参数的设置可以参考一下，是通用的解法，选取20%的token替换为mask token。
关于组会上提到两个loss预测token数量级的权衡，loss之间的权重配比，是否使用完整序列而非切片。看完这篇论文后仍存有疑问，是这周需要探索出来的主要工作

先学吧，巩固知识
结合场景，穿起来技术，不用啥都会，结合项目学深一点。参考ruoyi
搞个三层鉴权也行，
总分框架，重算法。重视用户引导和交互
docker k8s部署
一台机子上搞什么分布式（自己用就java -jar 最多加个nginx），知道原理就行了，问到就解释一下原因，说说自己的理解。
fastjson如何映射json到java对象，和tomcat有上面联系


mybatis如何连接数据库？ 通信方式是什么？ 接口和xml如何映射？ 返回数据如何映射成java类？


还有前端的axios拦截没有进行，还要放行create token路径，存token到cookie下次免登录（其实目前没必要搞，对于整个项目来说不是关键）

熟练的使用Hibernate、MyBatis等ORM框架，熟悉Hibernate和MyBatis的核心API，
对Hibernate的关联映射、继承映射、组件映射、缓存机制、事务管理以及性能调优等有深入的理解

session sqlSession和servlet有什么关系和区别
手写ORM框架，手写Tomcat框架/手写RPC框架
手写AOP redis cache 查询数据

rpc里就有远程调度，别搞什么聊天，没意义。就做一个支持热插拔spi的rpc dwmo框架就能学到很多东西，把性能提上去，面试都问

6月多逛逛论坛，现在专注基础
先把两本书翻烂，再复习笔记

什么技术不重要，重要的是任何框架都应专注于提供的<工具>和其可拓展性
上升的封装多个服务层级，本质都是服务间调用/通信，在调用链哪一节点都可以解决，k8s在最顶层实现，宇宙的尽头

链表题型3有很多可以做

科研中的收获，老师的真言：
0.只要不创新，有资料啥困难都能克服
1.人歇机器不能歇，首先效率拉满，其次拉满一次工作时限（干个爽，不要间隔中断思路）
2.多动脑子想好了再写代码，写代码是就最简单的事。结合现实思考本质依赖的原理/规则
3.看过什么做笔记，多复习笔记，书读百遍
4.积极交流，参考别人对问题的思考和解决
5.完整体系的知识点能通过ppt讲出来(站在别人角度，讲别人能快速理解的话<对应4>，最好方式是制造场景，指明需求，讲例子)。3+5就是记忆掌握的关键，记忆曲线不断回顾+转成自己的语言讲出来
*.总是着急去做，失败了就放弃去做别的了。应该要专研其本质，分析失败的原因是自己理解/用法有偏差，相信存在即合理特别是受到广泛认可的文章。

最近太忙，不能玩了

必看：
必看中必看:小林coding，java guide中java

txt语雀

计算机资料/
美团技术年货
阿里java开发手册

Java-常用知识&面试题库/
2022年06月精心整理版/学习手册&开源电子书
2022年更新-Java专题分类/
2022年11月收集/计算机基础
2024年更新Java面试指南(配套Java面试突击-V5.0)

论文看明白了就硬套一个文献间关系分析模型，将轨迹替代文献，出版数据为轨迹出发时间，关键词就是轨迹上道路节点，以此来获取关键词（道路节点）之间的关联，不如我们的一些图模型+序列模型，在路网限制基础上引入道路转移信息。关键在可视化和解释性方面，可视化方面能得出一个节点带权值的无向连通图，节点就是道路，权值越大代表高中介中心，用来基于当前流量做拥堵预测。道路拥堵与交通量无关，道路存在爆发状态和具有低中介中心性是交通拥堵的关键因素，而高中介中心性则是导致城市快速路交通拥堵的关键因素

分布式 KV（key-valu） 缓存系统，某个 key 应该到哪个或者哪些节点上获得，应该是确定的，不是说任意访问一个节点都可以得到缓存结果的。
12306

0.AOP+redis实现缓存功能
1.zookeeper+clickhouse实现分布式数据存储和查询
2.flink+消息队列 异步输出文件
可无：docker+k8s 项目部署

```

```
Typora恢复
C:\Users\wolf\AppData\Roaming\Typora\draftsRecover

Nacos后台启动
D:\Program Files\nacos-server-2.3.2\nacos\bin
startup.cmd -m standalone
http://localhost:8848/nacos/
Ruoyi替换了application.properties 修改了mysql持久化（保留了原始副本）

Sentinel后台启动
D:\Program Files\nacos-server-2.3.2
java -jar sentinel-dashboard-1.8.7.jar
java -Dserver.port=8718 -Dcsp.sentinel.dashboard.server=localhost:8718 -Dproject.name=sentinel-dashboard -Dcsp.sentinel.api.port=8719 -jar sentinel-dashboard-1.8.7.jar
后台8719默认
前台8080开启
http://localhost:8080/#/login
sentinel
sentinel


seata后台启动
D:\Program Files\nacos-server-2.3.2\seata\bin
seata-server.bat
http://localhost:7091/#/login
seata
seata

JMeter
D:\Program Files\nacos-server-2.3.2\apache-jmeter-5.6.3\bin
双击jmeter.bat

Copy Configuration 复制Provider另起端口9002，共享一个application name
-Dserver.port=9002

```



验证鉴权思路

```
版本：
java21(17) + spring.boot 3.2.0 + spring.cloud 2023.0.0 + spring.cloud.alibaba 2022.0.0.0
maven3.9.6 + mysql 8.0.36 + redis 5.0.14.1 + nacos 2.3.2 + sentinel 1.8.7 + jmeter 5.6.3
使用组件：
gateway + spring security + jwt + redis(nacos动态配置)
gateway:暴漏后台唯一端口，拦截全局请求，路由分发（lb负载均衡byApplicationName）
spring security:现成的验证和鉴权框架（自定义验证鉴权流程，exception handler处理未通过请求返回定制body），预设好强大的安全过滤链，service方法级的权限校验@PreAuthorize("hasRole("ADMIN")")
jwt:创建Authorization header token，保存用户信息和过期时间，服务端加盐（头部，载荷，签名），解析Claim
redis:保存角色-uri分支权限（nacos动态配置）
userdetails：保存用户名，密码，角色信息，对密码保护加密
ouath2: 授权第三方应用访问受保护资源框架

抽象流程：
1.暴漏微服务唯一对外端口（gateway），在到达具体controller接口前收集全局请求，过滤
2.自由放行白名单接口，用来create/refresh/validate token
3.拦截其余所有接口请求，认证+鉴权，解析jwt token(jwt excpetion token为空，格式不正确，解密失败，token过期)，对比userdetails.loadByUsername获取到角色权限，reids中获取角色下uri权限，对比请求uri分支有无交集，放行
4.catch抛出的各种类型exception，定制response

gategway与web互斥，内置webflux，响应式构建异步、非阻塞请求。能够处理高并发请求，效率远高于web

AuthenticationManager & AuthorizaitonManager：自定义认证，鉴权流程

SecurityExpressionRoot:@PreAuthority注解实现类，验证principal中的getAuthorities

SecurityContextHolder.getContext().setAuthentication(authenticationToken);获取当前线程中ThreadLocal保存的用户权限信息，但是在微服务调用链中某个环节可能会新建/覆盖线程丢失权限信息（controller->service）
管理SecurityContext. 管理对象SecurityContextHolder。MODE_THREALOCAL: 允许每个线程在安全上下文存储自己的详细信息，

spring security6.2缺点：
1.面向场景主要还是servlet-template前后端不分离，更别提微服务了，权限信息无法一直跟随调用链传递
2.集成度太高，流程刻板，自定义困难，可查阅文档和开源项目少
3.过滤链冗余，有些自定义的甚至重复过滤，增加系统负担

失败探索:
1.开源项目目录结构太复杂，调用关系太复杂，找不到切入点，只能从spring security官方文档的demo入手，但官方文档也不符合开箱即用的理解，还是前后端不分离的
2.找切入点找了很久，一直在spring security开源项目的代码上浪费时间。应该首先理清整个验证链逻辑，从gateway入手拦截到各个微服务请求，又得知spring security过滤在gateway之前，正好验证白名单放行，其他访问请求需要走一个验证流程。接下来就是自定义这个验证流程，create jwt token，解析token，对比权限
3.SecurityContextHolder.getContext().setAuthentication(*): 当前线程中ThreadLocal保存的用户权限信息，但是在微服务调用链中某个环节可能会新建/覆盖线程丢失权限信息（controller->service）——未解决，考虑在AuthenticationManager中把setAuthentication(*)方法绑定到调用链上（参考demo6, cloud-gateway:JwtAuthenticationManager）
4.鉴权从具体微服务sevice method层转移到url层中，只要在任一url传递链上进行过滤就行了
```



```
SecurityExpressionRoot
AuthorizationManagerBeforeMethodInterceptor
ThreadLocalSecurityContextHolderStrategy

——————
UsernamePasswordAuthenticationToken
UsernamePasswordAuthenticationToken
DefaultMethodSecurityExpressionHandler
AnonymousAuthenticationToken

——————————
normal
null
DefaultMethodSecurityExpressionHandler
UsernamePasswordAuthenticationToken

spring-security JwtAuthenticationManager（ReactiveAuthenticationManager）.addFilterAt(authenticationWebFilter, SecurityWebFiltersOrder.AUTHENTICATION);
```



```
您好面试官，我先自我介绍一下，我叫付珂，今年24岁，家在山东济宁。目前位于山东省济南市，就读于山东大学软件学院攻读硕士学位，预计25年毕业，正在寻求一份前端开发的暑期实习，对于实习地点没有要求，实习时间可以从现在开始到春节前结束。

tcp长连接需要服务端操作系统分配一个线程来维持吗，多路复用中的多路实际物理存在形式是什么？
1.在一个TCP长连接中，它的状态是由操作系统内核中的一个文件描述符来表示的，而不会为每个请求都创建一个新的文件描述符。当有新的请求到来时，操作系统会更新当前TCP连接的文件描述符的状态，而不是创建一个新的文件描述符。
2.对于事件驱动模型如epoll，当一个TCP长连接上有数据到达时，操作系统会更新该连接的文件描述符的状态，通知应用程序准备好去读取或写入数据。应用程序通过检测这个文件描述符的状态变化，能够及时进行相应的操作而不是为每个请求创建一个新的文件描述符。
3.多路复用技术实际上是通过一个线程同时监听多个文件描述符（套接字）的 I/O 事件，当有事件发生时，通知应用程序进行相应的处理。这样可以避免为每个连接分配一个独立的线程，提高系统的并发处理能力。

CPU不能直接访问物理内存地址，通过操作系统划分出一部分高速虚拟内存，间接映射，一是由于程序大小不可控，在用户感知上需要划分出足够大的逻辑区域，也就相当于对物理内存扩容，由于程序可分性，理论上可以通过局部性特性在一块限制大小的内存区域上通过换入换出实现承载超过区域大小的效果。二是不直接操作物理内存，是考虑到多线程并发的情况，开辟一块缓冲地带，交由操作系统规划对同一块物理内存的读写顺序。
如果说上面是软件逻辑层面对空间和资源在保证正确性下的更高效利用。CPU高速缓存就是在物理上加速其原子操作，不受操作系统调控，机械式地缓存连续空间，缩短两端速度不匹配造成的一方等待问题，同时为了提高缓存命中率，可以在编写程序时考虑该情况，尽量使用连续空间，逻辑分支可预测。
内存的碎片化和泄露问题。可以通过对于内存空间的逻辑组织尽量消弱，因为释放后的无论复制还是整理都无比低效，只能用分页方法用足够小的页作为划分组织单元，尽量减小实际浪费。分页的前提是基于程序/数据可分+容忍地址不连续带来的频繁IO的开销（因为碎片本质上还是存在，只是这里能利用起来，代价就是碎片不连续，不能一次获取到连续的空间，需要指针在行之间来回挪动，类似用遍历a[j][i]）

malloc只是在开辟用户态内存中原有预留出来的空闲地址，实际存在但逻辑上未被使用

一切存在的需要载体的，但载体都是有边界的，而货物理论上是可以无限增长的也就是总会大于边界，载体的扩充伴随开销，合理利用资源，花多少钱办多少事就是一个权衡问题，没有最优解。冯诺依曼体系结构。内存有什么用，解决双端速度不匹配，但这种解决思路不能无限套娃，必须在一处终结，那就是内存。首先要保证基本的承载需求，程序或者数据的大小是可以无穷大的，但现实世界物理存储是要有边界的，存储足够大的空间（2T），就面临速度差极大，优点就是空间是内存的数倍足够覆盖内存换入换出的需求
1.存储是有边界的，而且硬件是你不能决定的（或者上位分配的资源足够应付派发的任务）
2.内存换入换出的目的还是基于内存大小，协商一块面向程序和数据的逻辑边界，在限制了无界数据的大小的情况下满足任务能够完成（在边界下能完成是因为本身是线性可分的——局部性理论，存在部分之间不会相互影响的最小单元且完成子任务可以推进主任务进度至完成，可微），IO速度也有保障，还有硬盘给他托底
3.一旦内存实在紧张，频繁换入换出导致恶性循环，负载非但没有减轻反而加重，就会崩溃OOM
4.32位3G 64位128T。虚拟内存页也会占内存，实际到不了128T就会OOM，还是要看实际物理内存，开启swap就可以是128T。32位3G申请不了8G是因为3G就是虚拟内存的极限了，超过了地址空间都没了，分配了也找不到

预读机制：靠近当前被访问数据的数据，在未来很大概率会被访问到。缓存污染：全表扫描
```



```
ACM模式：新建数据结构类，建议与原始类并列写在下方，内部静态类，内部非静态类（需要先实例会外部类再实例化内部类）
外部并列类：在同一个源文件中并列写多个类，但只能有一个公共（public）类，并且该类的名称必须与文件名相同。如果有多个非公共类，则可以在同一个文件中并列写它们，但它们的可见性只在同一个包内。
静态内部类：首先跟其他静态方法调用一致，特点在main方法中可以直接实例化（不需要先实例化外部类，或者通过外部类类名引用），可以被正常实例化成对象，可以被GC回收
匿名内部类：实现接口或抽象类的实例化，new Interface(){@Override...}
Lamda表达式：原理与匿名内部类一致，写法简化了，同时只能实现一个接口的方法

private vs 不加修饰符
private：只能在声明它们的类内部访问
默认（不加修饰符）：在同一个包内可见，对同一个包中的其他类是可见的
1.private是最小权限，默认次之
2.同一包不一定是指在同一文件夹下，指在逻辑上的统一命名空间下，只要声明了同一package地址即可
```



```
关于继承的原理，子类和父类之间的关系
面向对象特点封装，继承，多态。继承都是单继承，拥有父类的所有变量和方法，可以重写(也就是重名)，引用时会先在子类内部再到父类(不会找父类的父类)
重名也可用this,super，无前缀(局部变量)来区分
子类构造函数默认实现父类无惨构造函数，隐式实例化
重写是默认的(检测重名，但是有两小一大的限制)，@Override只起伪代码作用，方便阅读父类不存在会报错

ArrayList源码
randomaccess需要空间连续，因此arraylist都是以开辟一块全新的连续空间来扩容
默认空，基础容量10，自动扩充1.5倍，dataElement.legnth>=size

HashMap源码
hashmap空间容量以2^n为单位，-1=尾数都为1，&运算就等价于(n-1)&hash=hash%n，且位与运算远快于取模

泛型
泛型编译时不确定，运行时才确定。类内只能保证派生自object类，因此只能调用object方法。编译时感知不到谁在调用实例他，运行时才知道
<？extends Comparable>就是想在Object之外调用一些其它类方法，就用类绑定限定泛型父类，同时可以调用父类方法——可以绑定类和接口，因此可以&多绑定
区分?和T，T只能用于声明，不参与实例化。?只用于填充。Point<?> point；

目前项目的数据数量级level：
时间：车数量-点数量-轨迹车数量-轨迹数量
2021-02-01：120w-300w-25w-100w
02/01-02/28：400w-7.5y-245w-6143w
卡口选取：47km*22km 2424个（有经纬度的1880）
出租车：2023.4-5月 6829辆出租车，gps采样率15s 
一辆车一天平均3500个点，30个订单，10小时，250km。一个月90w个点，600个订单

数据库和中间件选型和比较
clickhouse vs hadoop+hbase vs kudu
京东轨迹数据库细节（组织架构和应用场景）

```



```
排序算法:
选择，冒泡，插入：都是n^2，但如果原数组局部有序，冒泡和插入能优化到n，而且插入的原子操作比冒泡的swap简单

局部有序的情况下，冒泡能提前终止，插入能线性排序的原因
冒泡：尾部序列位置绝对正确的情况下，首部相对有序即可中断（两两之间&&有序）
插入：保证首部相对有序，尾部元素不破坏有序序列即可，且插入首部序列时可提前终止

希尔比插入好在哪（n^2 -> n^1.3）：
插入排序移动效率低，特别是较小值集中在后半段时，提前跨步交换在期望上能缩短时间
最差情况，每次局部排序对整体收益不会有负面影响（原理是每次交换的步骤，在插入排序中都是必然的，且compare操作耗时可不记——只受数组长度数量级影响，能尽早在大跨步下完成交换都能缩短时间）

自适应性：每次操作都是对于整体排序都是正向的，因此可以简化后续操作（冒泡和插入的提前终止，选择就不具备）

快排：快排是排序算法中最合适的，时间复杂度O(nlogn)，空间复杂度O(1)*递归栈帧O(logn)，最差O(n)。非稳定。
时间复杂度优于n^2算法（优化了操作总量，一步到位），空间复杂度又优于nlogn算法，由于局部连续的特性比堆排序对于元素访问顺序快。
最差情况是完全有序数组，划分永远是0和n-1子数组，时间复杂度O(n^2)，同时空间复杂度受递归栈帧影响到O(n)

归并：时间复杂度稳定，空间复杂度都是O(n)
堆排序：非稳定，访问元素开销大。适合作为优先队列和topk
快排：缓存使用效率高：在执行哨兵划分操作时，系统可将整个子数组加载到缓存，因此访问元素的效率较高。而像“堆排序”这类算法需要跳跃式访问元素，从而缺乏这一特性。

桶排序，计数排序，基数排序：
基数排序和计数排序都可以看作是桶排序。
都是类似hash，按索引存放元素再合并，需要已知单边边界，按步长划分索引范围。区别是计数是一种特殊情况，不需要重排序桶内元素，将元素直接映射到hash索引，统计数量，限制是元素需要为整数（hash key可数且能覆盖到可能出现的所有元素），且范围小（key数量不太多，O(n+m)m如果过大就很慢）。要求非负整数是hash中key值无序，数组下标有序但需要从0开始，将count作为累计offset（前缀和）
基数排序是从个位开始，逐位进行计数排序，特点是可以覆盖的范围大，但位数不能过大，时间复杂度是O(nk)
稳定性：计数排序counter前缀和计算出来的下标在末尾，取其中的元素是顺序取从0，因此要倒叙遍历原数组放入矩阵行中

循环不变量：跟目标结果有关，是迭代中不变的性质，用于理清思路。举例：原数组重组元素，外层遍历数组，内层按条件指针移动，指针即为不变量（不随外层遍历而改变）

排序算法的思想潜藏着各种解题思路：
1.归并排序（merge sort）拆分再合并的思想，比暴力遍历n^2要省时
```



```
2024/4/30-5/3 上海学习记录
Redis AOF(Append Only File)和RDB 数据持久化
持久化写入时的数据一致性问题：
1.只读：靠虚存来共享页表，指向同一位置
2.缓存被修改：写时复制，启动AOF后台进程，写时复制跟mvcc快照类似先写入某时刻缓存快照，再写入过程中被修改的缓存
RabbitMQ 消息队列中间件
为什么要使用：
1，无论是消息队列还是微服务RPC—openfeign。关键原因就是服务解耦，如果没解耦，调用链某一点报错整体都错了，想看到具体哪里错了还要一个函数调用写容错(类似try catch)。现在我想统一一下容错方式，定位到哪个调用链上出了问题，还知道显示前面的没错，又想避免繁多的容错逻辑就用微服务或消息队列解耦合，错误信息统一回复

amqp是协议，有一套固定的框架和规范元素（Provider, Consumer, Exchange, Queue, Binding）

RabbitMQ实现流程
1.rabbitmq 生产者通过exchange路由和路由键与消费者相连，消费者还要额外指定queue来接收用于持久化和查看抽象等待队列，因为是异步所以需要可见性相当于consumer name（但不完全是consumer_name,因为一个queue可以对应对各consumer实现负载均衡）
2.queue是消息的容器，同一队列名可以实现负载均衡，round_robin轮询
3.queue和consumer的关系是一对多，反过来是一对一。也就是先判断exchange再判断key，两个consumer具有不同的exchsnge和key匹配规则，但有相同的queue，来自不同consumer的消息消息会被存放在同一个queue，之后实现负载均衡只由其中一个来消费。因此queue可以看做是基于exchange和key之上抽象引入的多路复用的思想
4.什么情况会导致消息乱序。处理顺序123，绑定多个consumer到同一个queue上了，有的返回快有的返回慢返回就按132。解决，有序消息，放入同一队列中只由一个consumer来处理，或者内部设阻塞锁。放入的顺序是123，返回的顺序132。假设单个consumer遵循单线程串行化执行模式，queue符合队列数据结构特性

为什么需要分布式集群（为什么单个节点会崩溃）
1.同一时间大量请求会使分布式节点或微服务程序崩溃，触发了oom进程被操作系统干掉了，或用户请求被降级熔断被排队等待逻辑上停滞了，或者物理层面被关闭(因为操作系统不会主动关机)。
2.操作系统层面进程崩溃分为内存回收被kill，cpu线程创建或任务堆积被kill。内存层面是硬盘换入换出恶性循环(频繁io开销本身属于额外的，开销又大)直接回收也没有足够的空间(后台回收，回收页分为文件和匿名，回收时机优先级，)

心得：
1.复习主要反思，谦虚的心哪里学的不足，以点来扩展，不要以面来浏览。主要精力探索新知识和干活
2.书读百遍，其义自见
```



```
2024/5/3 优化gateway+security+jwt统一验证+鉴权机制
之前弊端：
1.Authentication被跳过了或实际没有作用，不参与过滤链。只有Authority在工作，原理是放行/auth和/test，其他jwt请求解析请求header中的Authorization token，比对内置的User存在且没有过期则通过，否则鉴权失败交给EntryPoint返回结果
2.@PreAuthorize失效，因为Authority过程中无法保存加载的User ROLE Authority到过滤链中，只能生成一个匿名Authticattion User且无权限
3.具体模块的实际功能不符合spring security预设的期望功能，也就是能跑出期望的结果但代码不健壮，没有弄清security实际的原理和流程，不符合规范之后拓展功能留下隐患

改进：
1.设计Authentication，使其发挥出应有的作用，而不是把一切都堆到Authority，其只负责鉴权，也就是比对uri/path和ROLE_Authority
2.TokenAuthenticationConverter默认在开头解析请求header中的Authorization token，并加入到Authentication中之后的过滤链中能访问到该token。此时token为空，会自动生成一个匿名无权限的token，在之后鉴权中会拦截，这一步是考虑到/auth没有token，在鉴权中才能根据uri白名单放行，但其他uri会因为匿名权限被鉴权拦截
3.AuthenticationManager负责解析token，并根据解析过程中的各种情况携带Message抛出异常，由GlobalHandler处理返回Response。成功解析出的UsernamePasswordAuthenticationToken保存到过滤链中能随时获取到
4.AuthenticationWebFilter携带AuthenticationManagervert加入到过滤链Order.AUTHENTICATION位置
5.AuthorizationManager一部分是白名单uri放行，其余需要被鉴权。拿到AuthenticationManager中保存的AuthenticationToken，get到用户的Authority和请求中的uri，拿到redis中保存的uri-ROLE，验证是否存在交集判断是否放行。不放行则交给AccessDeniedHandler处理

经验总结：
1.别人项目的源码没有完全弄懂就着急写代码，应该要先理清整个框架，过滤链上的各个组件都扮演着什么角色，不要弄混，有了抽象的认知才能找到切入点逐个完成
2.重点还是思考>写代码，遇见困难一定要琢磨透一条线都理清楚了在开始写，可以记笔记或画图来记录思维脉络。同时遇见难点应该发散着思考一下，他的前置条件都有什么，为什么我调用不到该对象，是不是前面哪里漏了一步，我应该怎么实现该前置条件，前置都准备好了问题就是迎刃而解了copy代码就行了
3.谦虚+坚毅不倒，现阶段的做法感觉不对劲有些问题，可以先放一会但一定要契而不舍地攻克他。还是思考>坐在电脑前面对代码发呆，可以刻意去干别的，留出空闲时间好好思考，问题总会解决，不要陷入死胡同
4.commit保存先有能运行的结果，再继续探索试错，方便回滚，试错过程注意代码整洁性。

2024/5/9
Redis 检验url + AOP @RequiresRoles
@RequiresRoles({"ROLE_ADMIN", "ROLE_USER"})
常见的鉴权方式有两种：
1.正规的：gateway统一拦截请求 + security认证鉴权流程
2.自定义：AOP在业务方法处拦截请求，校验注解值和token权限有无交集

```



```
Spring源码解析
总：Spring是基础框架，提供Bean的容器，方便装载具体的Bean对象（之前使用需要具体new实例化对象，现在只需要告诉容器有哪些对象，帮我们创建并维护生命周期）。其上可扩展出完整的生态（spring boot, spring cloud...）
分：
IOC(Inversion of Control):
通常需要自己new一个对象，现在交由SpringBean容器来统一创建、管理生命周期
AOP(Aspect-Oriented Programming)：
开发业务逻辑之外的可扩展功能，日志、权限控制和事物等

IOC:
思考：
总的需求->
使用spring最方便的地方，在于不需要手动new对象，而由容器帮我们创建，并放到容器中。

用的时候怎么取出->
ApplicationContext.getBean(Person.class)，一堆Map方式存储Bean对象，循环依赖-三级缓存-三个Map结构
容器怎么知道需要创建哪些对象->xml or 注解。

->如何被容器识别并解析
xml: io stream读取到内存->SAX/dom4j(解析)->document(文件结构对象)->包含一系列node节点->解析key/value 放入某个对象中
注解: 反射->class loader->class对象.getAnnotation->判断是否加了spring特性注解@Controller@Component@Service->创建对象放入容器中
解析规则不一致但最终解析成同一个东西，接口定义不同规则（约束和规范，BeanDefinitionReader）
总结：定义创建Bean对象所需要的基本信息

举例：
基于xml的配置：对于路径，文件名，文件拓展名没有任何强制要求。依赖主配置文件(通常是web.xml)或spring java配置类，来指定要加载的xml文件路径
<context-param>
    <param-name>contextConfigLocation</param-name>
    <param-value>classpath:applicationContext.xml</param-value>
</context-param>
<listener>
    <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
</listener>
这里的contextConfigLocation参数指定了Spring配置文件的位置。你可以在其中列出多个文件，以逗号或空格分隔。

基于注解：来导入XML配置文件
@Configuration
@ImportResource("classpath:applicationContext.xml")
public class AppConfig {
    // 配置类的内容
    // @Import(AppConfig.class) @Import用于导入其他的配置类
}

->装载到容器中
Map结构存放(BeanDefinition) BeanFactory.BeanDefinitionMap(Bean定义信息)。BeanDefinition是接口，定义一系列具体的规范

-->创建完整的BeanDefiniton对象(可选，可扩展)
一系列BeanFactoryPostProcessor来set属性值，替换占位符等

->创建Bean对象：反射（效率比较低为什么用，有条件的当需要创建的对象非常多才效率低）
1.获取Class对象
Class clazz = Class.forName("包名.类名")
Class clazz = 对象.getClass();
Class clazz = 类名.class;
ClassLoader.getxxxClassLoaderr().loadClass("包名.类名");
通过类加载器获取 Class 对象不会进行初始化，意味着不进行包括初始化等一系列步骤，静态代码块和静态对象不会得到执行。仅仅获取了类的定义信息，而没有触发类的初始化过程。这种延迟加载的特性允许你在需要时才初始化类，节省了资源
2.获取构造器
Constructor ctor = clazz.getDeclaredConstructor();
3.创建对象
Object obj = ctor.newInstance()

->使用Bean对象
->销毁Bean对象

=============================================================
spring作为基础框架、基石的可扩展性。以BeanFactoryPostProcessor为例进行阐述

PostProcessor增强器，提供某些扩展功能。针对处理对象不同，分两类如下：
BeanFactoryPostProcessor -> BeanFactory
BeanPostProcessor -> Bean
什么是BeanFactory：
定义：访问SpringBean容器的root接口，功能上是bean对象创建的整体流程，Bean生命周期是一个完整标准化流程，相对比很麻烦。流水线工作
举例：ApplicationContext.getBean(Person.class)
1.ApplicationContext实现了BeanFactory，通过ApplicationContext访问容器中的Bean实际通过BeanFactory访问容器。
2.同样也可以通过BeanFactory来修改Bean，如下
什么是FactoryBean接口：
定义：用来创建Bean对象。私人定制
区别：
1.isSingleton: 判断是否是单例对象
2.getObjectType：获取返回对象的类型
3.getObject：创建对象 1)new 2)反射 3)动态代理

举例：对于xml文件 <property name="username" value="${jdbc.username}"/>
BeanDefinition需要调用PostProcessor来替换占位符
BeanFactoryPostProcessor.PlaceholderConfigurerSupport实现类替换上面的${}占位符(db.properties)
总结：
1.通过BeanFactoryPostProcessor定义完整的BeanDefinition对象
2.可扩展性=可以任意实现BeanFactoryPostProcessor来自定义修改BeanDefiniton（set属性值，替换占位符），并加载到容器中。可以@Order调整PostProcessor顺序
3.日常业务流程中一般用不到，二次开发中会用到

举例加载Bean过程的可扩展性，先有的xml方式来帮助容器识别定义Bean，后来的注解方式如何基于该流程做扩展？
xml->标准解析处理流程
注解->是推到全部重写 or 原有基础上做扩展 ->@ComponentScan @Import @ImportResource
1.注解扮演的角色就是在xml标准解析处理流程基础上导入额外对象
2.具体依靠BeanDefinitionRegistryPostProcessor：扩展原有的xml流程到注解，额外通过注解注册Bean

Interface——接口带来的可扩展性
举例：比如实现BeanDefinition接口，可以实现从不同文件格式读取Bean定义信息（不只是xml），json/properties/yml
接口和抽象类区别：
1.抽象类单继承，接口多实现。抽口定义不同规则（约束和规范）
2.内部只能有抽象方法，抽象类可以有具体的实现方法
（上面是语法上的区别，想问的是功能需求上的区别）
3.接口是自上向下的，抽象类是自下而上的，接口不需要考虑后面具体的子类实现，抽象类要抽取出子类的共性的。

除了接口还有上面可以实现可扩展性->设计模式：模板方法
举例：spirng boot嵌入tomcat，通过实现模板方法（模板方法留空带实现）
==============================================================

之前讲的都是Bean的识别和加载到Bean容器的过程，包含完整BeanDefinition的创建，是Bean对象创建的前置过程。接下来才是Bean对象的生命周期，是在Bean容器中进行的

Bean生命周期：从对象的创建到使用到销毁的过程
实例化和初始化的区别：new和setXXX()
->实例化：在堆空间中申请内存空间，对象的属性值一般是默认值。->createBeanInstance反射创建对象
->初始化属性赋值：给自定义属性赋值->populateBean.set方法完成赋值操作
->初始化属性赋值：检查aware相关接口并设置依赖（给容器对象属性赋值，类似于依赖注入）：Aware意识到，里面没有定义任何方法。->invokeAwareMethods完成赋值操作

为什么要识别Aware，Aware的主体是什么：
1.初始化就是避免成员变量为空，因为为空就没有任何意义，需要set成员变量，可以是直接值传递也可以是间接引用传递。而间接引用需要确保首先存在实例，其次知道去哪找。
2.下面可知容器对象应该是先于自定义对象被加载到容器的，属于公共资源，因此存在实例。对于那些可能要被注入/依赖的容器Bean对象，需要提供对外开放的接口/标识，便于找到对应的Bean
总结：
1.定义的成员变量，属于Bean容器中的公共资源，Bean容器需要提供对外开放的Aware接口/标识，才能在初始化赋值过程中被访问到
2.Aware主体是Bean容器的公共资源，也就是预先创建好的容器对象
spring中按照bean对象的使用者分为几类：
1.自定义对象
2.容器对象（BeanFactory, ApplicaitonContext, Environment）
谁在什么时候调用这些方法（Aware:set成员变量）？
容器 在 需要给一个统一的标识，然后在统一的地方进行处理

执行到此步骤之后，对象的创建和属性赋值都完成了，此时对象是否可以直接拿来使用了?
理论是可以的，但注意Spring要考虑扩展性
->获得普通对象，是否需要进行扩展（挂载预定义好的AOP，做Bean对象的扩展实现）
->执行前置处理方法：AOP.BeanPostProcessor.BeforeInitialization
->执行初始化方法：invokeInitMethod(90%用不到，判断逻辑监测bean是否实现了InitalizingBean接口->调用afterPropertiesSet方法（自定义额外工作）)
->执行后置处理方法：AOP.BeanPostProcessor.AfterInitialization

AOP原理：
动态代理：jdk和cglib
AOP的入口就在此处：后置处理方法
AOP挂在IOC流程中的Bean初始化过程中，也是IOC整体流程中的一个扩展点（本质是Bean的生命周期，创建流程，IOC全程参与是个抽象的概念）

代理模式：
静态代理：实现相同接口的两个类，一个业务类Service，一个代理类Proxy，实现同一个方法，Service作为Proxy成员变量在实现方法中调用自己的方法，而Proxy就在该方法前后添加业务逻辑
动态代理：从JVM角度来说，动态代理是在运行时动态生成类字节码，并加载到JVM中的。Spring AOP、RPC的实现都依赖了动态代理
1.JDK动态代理只能代理实现了接口的类或者直接代理接口，而CGLIB可以代理未实现任何接口的类。 另外，CGLIB动态代理是通过生成一个被代理类的子类来拦截被代理类的方法调用（Enhancer），因此不能代理声明为 final 类型的类和方法。
2.就二者的效率来说，大部分情况都是 JDK 动态代理更优秀，随着 JDK 版本的升级，这个优势更加明显。


此时可以使用Bean对象了
->使用Bean对象
->销毁Bean对象：从不使用，不是GC，关闭Bean容器时销毁->context.close()

源码没有那么难，只不过大家看不下去：抽丝剥茧，保留线性主干再去扩展
```



```
循环依赖
Bean默认单例模式singleton(即配置一个Bean对象后，IOC容器只会创建一个该Bean的实例，后续使用的是同一个实例)，还有原型模式prototype(多例模式，每次请求 Bean都会创建一个新的实例)，还有基于Request，Session，GlobalSession的多例模式，还可自定义模式
A(b) <-> B(a)
同时自定义Bean如何作为成员遍历被初始化
三级缓存，提前暴露对象

创建A对象->实例化A对象->给A对象的b属性赋值/属性注入->A，B都是单例，先去容器内查找，是否有B对象->有即返回，没有即创建B对象，流程和创建A对象一致->...->B：去容器查找是否有A对象

将对象按照状态分类：
1.成品->完成实例化和初始化
2.半成品->完成实例化但未完成初始化

当持有某一个对象的引用后，能否在后续步骤的时候给对象进行赋值操作？可以，也就是切断最后一步形成闭环步骤
实例化和初始化可以分开执行，破坏死锁条件，重点在预判死锁的形成，阻塞其他请求，并实例化后缓存该状态，初始化后再缓存该状态，完成之前实例化引用的赋值。因此上面死锁状态更新为如下流程：
实例化A（map A:半成品）。实例化B后缓存B(map B:半成品)，初始化B时直接赋值缓存的A:半成品，缓存B(map B:成品)，回到初始化A返回B:成品，缓存A(map A:成品)

上述缓存map中一个Bean存在两种状态的key-value，冗余。因此有了三级缓存，3个map结构
一级：Map<String, Object> singletonObejcts ConcurrentHashMap.最终形态的单例bean,我们一般获取一个bean都是从这个缓存中获取
二级：Map<String, Object> earlySingletonObjects ConcurrentHashMap
三级：Map<String, ObjectFactory> singletonFactories HashMap
ObjectFactory:函数式接口，java8，可以将lambda表达式作为参数放到方法的是惨重，在方法执行时，并不会实际的调用当前lanbda表达式，只有在调用getObejct方法的时候才会调用lambda表达式
通过调用ObjectFactory的getObject()方法，就能够在需要动态代理的情况下为原始对象生成代理对象并返回，否则返回原始对象

三级缓存当中创建对象是需要牺牲一定得性能,二级缓存的作用防止在多级循环依赖的情况下重复从三级缓存当中创建对象
只有在调用了三级缓存中的ObjectFactory的getObject() 方法获取原始对象（的代理对象）时，才会将原始对象（的代理对象）放入二级缓存，而调用三级缓存中的ObjectFactory的getObject() 方法获取原始对象（的代理对象）这种情况只会发生在有循环依赖的时候，所以，二级缓存在没有循环依赖的情况下不会被使用到。二级缓存是为了提前暴露 Bean 来解决循环依赖问题，此时的 Bean 可能还没有进行属性注入，只有等完成了属性注入、初始化后的 Bean 才会上移到一级缓存（单例池）中。
根本目的是想map分开保存成品和半成品bean。而只有循环依赖的情况下才会用到半成品bean，因此需要代理对象能直接获取到单例半成品bean，因为多级循环依赖创建代理对象开销大又引入二级缓存保存创建的代理对象

这里为什么用objectFactory代理对象而不是直接保存原本的半成品bean对象？
1.当 Spring 在实例化 A 类时，会将 A 类的半成品对象放入第三级缓存中，同时返回一个代理对象，这个代理对象负责处理 A 类中对 B 类的依赖，完成B的初始化后可以通过代理对象回调完成A的初始化。
2.目的是延迟 Bean 的初始化过程，解决循环串行化必要的，B初始化完成后回调上一步A半成品继续初始化

Bean不同创建模式
1.配置的Bean默认scope="singleton"（即使你没有手动写出），若该Bean没有进行任何其他配置，默认会在容器启动时创建该Bean实例，并放入到单例池beanFactory-->singletonObjects中
2.若配置Bean时指定了scope = "prototype"，则Bean实例被创建的时机会被推迟，该Bean只有在getBean(...)时才会被创建
3.Bean实例的创建时机，还取决于Bean的lazy-init (懒加载)属性；默认lazy-init="false"，即默认Bean实例都是在容器启动时就创建（以空间换时间）。
 ->对于 scope ="prototype"的情况，不管lazy-init=true还是false，Bean实例都是只有在getBean(...)时才会被创建。而
 ->对于scope="singleton"的情况，若在单例形式的同时，希望Bean是在getBean(...)被调用时创建，则可以指定懒加载lazy-init="true"。

```



```
docker
定义：Docker是一款可以将程序和环境打包并运行的工具软件
基础镜像：选中一个基础操作系统Ubunutu和语言JVM后，我们将它们对应的文件系统，依赖库，配置等放一起打包成一个类似压缩包的文件，这就是所谓的基础镜像(Base Image)。
dockerfile:从操作系统到应用服务启动，需要做哪些事情的清单文件（todo list）
这里可以指定基础镜像即docker官方提供的开源镜像，基础镜像可以多继承，也可以指定其中的一些子类
容器镜像：执行 docker build 的时候，Docker 软件就会按着 Dockerfile 的说明，一行行构建环境+应用程序。最终将这个环境+程序，打包成一个类似"压缩包"的东西
镜像仓库：通过 docker push 将镜像推到仓库，有需要的时候再通过 docker pull 将镜像拉到机器上。这个负责管理镜像仓库推拉能力的服务（服务器那么多，挨个将容器镜像传过去也不是不行，就是将压力全给到发送方的网络带宽了，只能类似github，保存到中间平台，客户端自行拉取）
容器：执行 docker run 命令，将这个类似"压缩包"的容器镜像给"解压缩"，获得一个独立的环境和应用程序并运行起来。
docker vs 虚拟机：
1.传统虚拟机自带一个完整操作系统，而容器本身不带完整操作系统，容器的基础镜像实际上只包含了操作系统的核心依赖库和配置文件等必要组件，更加轻量化。
2.docker利用一个叫 Namespace 的能力让它看起来就像是一个独立操作系统一样。再利用一个叫 Cgroup 的能力限制它能使用的计算资源。
3.容器本质上只是个自带独立运行环境的特殊进程，底层用的其实是宿主机的操作系统内核

Docker Compose 是什么？
需求：想要部署多个容器，且对这些容器的顺序有一定要求呢？比如一个博客系统，当然是先启动数据库，再启动身份验证服务，最后才能启动博客 web 服务。按理说挨个执行 docker run 命令当然是没问题的，但有没有更优雅的解决方案？
定义：有。我们可以通过一个 YAML 文件写清楚要部署的容器有哪些，部署顺序是怎么样的，以及这些容器占用的 cpu 和内存等信息。通过一行Docker-compose up命令，开始解析 YAML 文件，将容器们一键按顺序部署，就完成一整套服务的部署。

Docker Swarm 是什么？
Docker解决的是一个容器的部署；Docker Compose 解决的是多个容器组成的一整套服务的部署。
需求：Docker Swarm 就更高维度了，它解决的其实是这一整套服务在多台服务器上的集群部署问题。
比如在 A 服务器坏了，就将服务在 B 服务器上重新部署一套，实现迁移，还能根据需要对服务做扩缩容。
扩缩容是根据当前流量需求，复制到另一台机器上运行，或着关闭某台机器上正在运行的容器

Kubernetes(k8s)
k8s会在多台 Node 服务器上调度 Pod，进行部署和扩缩容。每个 Pod 内部可以含有多个 container，每个 container 本质上就是一个服务进程。
Docker Compose 基于多个container创建的一整套服务，其实就是k8s里的pod。而Docker Swarm做的事情和k8s一样，本质上就是在调度pod。

应用部署方式的演变：
1.传统部署：直接将应用程序部署在物理机上，java -jar xxx.jar
缺点：无法自定义为程序分配资源，服务之间抢占相互影响
2.虚拟化部署：一台物理机运行多个虚拟机，每个虚拟机配置独立环境
缺点：增加操作系统，浪费部分资源
3.容器化部署：与虚拟化类似，但共享了操作系统，只保留所需要的配套环境、依赖和工作目录。演化成最终的轻量级，具备资源隔离，实现快速转移部署、扩缩容的分布式应用部署解决方案
优点：
可以保证每个容器拥有自己的文件系统、CPU、内存、进程空间等
运行应用程序所需要的资源都被容器包装，并和底层基础架构解耦
容器化的应用程序可以跨云服务商、跨Linux操作系统发行版进行部署


Prometheus
需求：Prometheus和Kubernetes有很多相通之处，Kubernetes其中一个功能是提供了弹性动态的部署能力，而Prometheus则提供了动态的监控能力。Kubernetes集群的扩容可以自动化是因为所有的资源对象，调度策略都已经在标准化的编排文件中声明了，搭配云服务器的自适应扩缩容以及Kubernetes提供的Horizontal Pod AutoScaler等机制，可以获得恐怖的弹性能力和运维自动化水平，而Prometheus这种能支持动态部署拓扑的监控系统成为主流。
定义：Prometheus是Google监控系统BorgMon类似实现的开源版，整套系统由监控服务、告警服务、时序数据库等几个部分，及周边生态的各种指标收集器（Exporter）组成，是在当下主流的云原生监控告警系统，Prometheus有这些特性：
1.开箱即用的各种服务发现机制，可以自动发现监控端点；
2.专为监控指标数据设计的高性能时序数据库TSDB；
3.强大易用的查询语言PromQL以及丰富的聚合函数；
4.可以配置灵活的告警规则，支持告警收敛（分组、抑制、静默）、多级路由等等高级功能；
5.生态完善，有各种现成的开源Exporter实现，实现自定义的监控指标也非常简单。

DevOps(Development & Operations)
DevOps中的Dev指的是Development（开发），Ops指的是Operations（运维），用一句话来说，DevOps就是打通开发运维的壁垒，实现开发运维一体化。
DevOps是一组过程、方法与系统的统称，用于促进开发、技术运营和质量保障（QA）部门之间的沟通、协作与整合。反正它不是某一个特定软件、工具或平台的名字。
```



```
消息队列Kafka——支持分布式架构和大规模数据处理需求
对比：RabbitMQ专注于消息传递，在传统的消息队列场景中表现较好

高性能：
1.同一队列，增加生产者和消费者提升消息吞吐量（但同时争抢同一个消息队列，抢不到的一方就得等待）
2.新增topic，对消息进行分类，每一类是一个 topic，然后根据 topic 新增队列的数量，生产者将数据按 topic 投递到不同的队列中，消费者则根据需要订阅不同的 topic。（但单个 topic 的消息还是可能过多，我们可以将单个队列，拆成好几段）
3.每段就是一个 partition 分区，每个消费者负责一个 partition。这就大大降低了争抢，提升了消息队列的性能。

高扩展性：
需求：随着 partition 变多，如果 partition 都在同一台机器上的话，就会导致单机 cpu 和内存过高，影响整体系统性能。
1.可以申请更多的机器，将 partition 分散部署在多台机器上，这每一台机器，就代表一个 broker。我们可以通过增加 broker 缓解机器 cpu 过高带来的性能问题。

高可用：
需求：如果其中一个 partition 所在的 broker 挂了，那 broker 里所有 partition 的消息就都没了。这高可用还从何谈起？
1.我们可以给 partition 多加几个副本，也就是 replicas，将它们分为 Leader 和 Follower。Leader 负责应付生产者和消费者的读写请求，而 Follower 只管同步 Leader 的消息。（主从模式）
2.将 Leader 和 Follower 分散到不同的 broker 上，这样 Leader 所在的 broker 挂了，也不会影响到 Follower 所在的 broker, 并且还能从 Follower 中选举出一个新的 Leader partition 顶上。这样就保证了消息队列的高可用。

持久化和过期策略：
需求：刚刚提到的是几个 broker 挂掉的情况，那搞大点，假设所有 broker 都挂了，那岂不是数据全丢了？
1.我们不能光把数据放内存里，还要持久化到磁盘中，这样哪怕全部 broker 都挂了，数据也不会全丢，重启服务后，也能从磁盘里读出数据，继续工作。
2.磁盘总是有限的，所以我们还可以给数据加上保留策略，也就是所谓的 retention policy，比如磁盘数据超过一定大小或消息放置超过一定时间就会被清理掉。

consumer group
需求：按现在的消费方式，每次新增的消费者只能跟着最新的消费 Offset 接着消费。如果我想让新增的消费者从某个 Offset 开始消费呢？（比如，下订单成功通过推送短信和邮件的方式推送消息，短信和邮件两个消费者消费同一个队列，但本身业务逻辑是异步的，没必要等待另一方的同步情况，只干自己的活就好了，就会有两个不同的指针在消息队列上移动）
1.给消息队列加入消费者组（consumer group）的概念，短信推送和邮件推送服务各自是一个独立的消费者组，不同消费者组维护自己的消费进度，互不打搅。

ZooKeeper
需求：每个组件(broker)都有自己的数据和状态，所以还需要有个组件去统一维护这些组件的状态信息
1.引入 ZooKeeper 组件。它会定期和 broker 通信，获取 整个 kafka 集群的状态，以此判断 某些 broker 是不是跪了，某些消费组消费到哪了。(服务注册，服务发现，服务治理)

总结：
1.凡是异步场景都能用到消息队列
2.比如上游流量忽高忽低，想要<削峰填谷>，提升 cpu/gpu 利用率。
3.又比如系统过大，消息流向盘根错节，想要拆解组件，<降低系统耦合>
4.再比如秒杀活动，请求激增，想要<保护服务>的同时又尽量不影响用户

解耦的好处:
开发:利于多团队协同开发，异步测试部署，敏捷开发而非瀑布。高内聚低耦合便于复用，统一规范接口文档，便于理解和维护
维护:防止局部异常导致整体链路崩溃，熔断降级异步返回错误信息并传递正常节点信息，流量控制分布式扩缩容，权限控制更加灵活
```



```
Dubbo vs OpenFeign
Dubbo：Dubbo是一款高性能的Java RPC框架。其前身是阿里巴巴公司开源的一个高性能、轻量级的开源Java RPC框架，可以和Spring框架无缝集成。提供了三大核心能力：面向接口的远程方法调用，智能容错和负载均衡，以及服务自动注册和发现。
相同点：Dubbo 与 Feign 都依赖注册中心、负载均衡，作用是提供远程接口调用。
常见的实现远程调用的方式： Http接口（web接口、RestTemplate+Okhttp）、Feign、RPC调用（Dubbo、Socket编程）、Webservice。
区别：Dubbo除了注册中心需要进行整合，其它功能都自己实现了，而Feign大部分功能都是依赖全家桶的组件来实现的。
1.协议：
Dubbo支持多传输协议(Dubbo、Rmi、http、redis等等)，可以根据业务场景选择最佳的方式。非常灵活。默认的Dubbo协议：利用Netty，TCP传输，单一、异步、长连接，适合数据量小、高并发和服务提供者远远少于消费者的场景。
Feign：基于Http传输协议，短连接，不适合高并发的访问。
总结：Dubbo支持更多功能、更灵活、支持高并发的RPC框架。SpringCloud全家桶里面（Feign、Ribbon、Hystrix），特点是非常方便。
```



```
hadoop分布式全家桶——类似spring具有生态的概念
需求：简单地说，Hadoop是一个平台，在它之上可以更容易地开发和运行处理大规模数据的软件。类似于操作系统提供的socket接口，在其之上可以扩展网络通信的各种功能，比如手写RPC框架。比如spring的bean容器，扩展生态都可基于自定义注解解析为bean。重点是对于某些特殊资源，覆盖了对其原子操作的集合，在其上可以做任意扩展。

HDFS：一个分布式文件系统，能够以高吞吐量访问应用中的数据。
Hive：数据仓库基础架构，提供数据汇总和命令行即席查询功能。
YARN：一个作业调度和资源管理框架。
MapReduce：一个基于YARN的大数据并行处理程序。
Spark：可高速处理Hadoop数据的通用计算引擎。
ZooKeeper：用于分布式应用的高性能协调服务。
Flink：一款高吞吐量、低延迟的针对流数据和批数据的分布式实时处理引擎，是实时处理领域的新星。

消息处理都有了，那想必存储也会有（hadoop全家桶）；有了文件总得查吧（这个可选就很多了，但是es跑不掉）；既然hadoop上了，yarn也不能浪费，干脆再来点spark
hadoop和es都上了，做大数据可视化不得用上Flink ，既然都用Flink了配套的存储池Paimon也得用上


```



```
ClickHouse vs HBase vs Kudu
ClickHouse:面向不涉及增删改的场景，具有高效的查询性能

ClickHouse vs Elasticsearch
1.Elasticsearch最擅长的主要是完全搜索场景（where过滤后的记录数较少），在内存富裕运行环境下可以展现出非常出色的并发查询能力。但是在大规模数据的分析场景下（where过滤后的记录数较多），ClickHouse凭借极致的列存和向量化计算会有更加出色的并发表现，并且查询支持完备度也更好。
2.ClickHouse的并发处理能力立足于磁盘吞吐，而Elasticsearch的并发处理能力立足于内存Cache，这使得两者的成本区间有很大差异，ClickHouse更加适合低成本、大数据量的分析场景，它能够充分利用磁盘的带宽能力。
3.数据导入和存储成本上，ClickHouse更加具有绝对的优势。
```



```
RuoYi-Cloud
外部鉴权：
外部不拦截，只在具体资源处用注解AOP拦截，验证Jwt token获取到用户信息包括权限（这一步是在调用链上是能获取到请求header的，ServletUtils.getRequest()通过spring内置的RequestContextHolder），对比注解中的value。前端会调用axios拦截器，发生请求前在header中添加上保存在cookie中的token
区别：我们是在全局拦截因此只能根据uri路径判断权限，ruoyi在资源处拦截就可以自定义权限，缺点是不安全没有统一鉴权

gateway yml里面的Path:/auth/** auth是前缀可以被替代，前提是加了filters
服务间用R，返回给前端用AjaxResult
ajax网页异步刷新，不需要刷新页面，发送请求后页面不会被阻塞
feign的rpc调用过期时间一般是60

Mybatis-Plus
分页几种方法常用的就是两种：
1.使用Page，IPage并配置MybatisPlusConfig
1)需要生成Page<?>对象作为mapper第一个参数，即自动分页。需要指定页码和页面大小
2)需要定义IPage<?>作为mapper返回对象，包括:total(record总数),size(页面大3)小),current(当前页),records(存放实际返回的泛型List)
3)注册配置Bean-MybatisPlusConfig
2.使用PageHelper组件
不需要添加上述任何东西，只需要紧跟着mybatis查询语句前面添加PageHelper.startPage(current, size);
只需要额外添加pom依赖
<dependency>
    <groupId>com.github.pagehelper</groupId>
    <artifactId>pagehelper-spring-boot-starter</artifactId>
    <version>1.4.6</version>
</dependency>

Wapper用法——不需要定义mapper接口和xml，可以直接自定义sql条件语句
但需要学习Wrapper函数和用法
LambdaQueryWrapper<Order> query = new LambdaQueryWrapper<>();
query.eq(Order::getUserNo, req.getUserNo());
PageHelper.startPage(req.getCurrent(), req.getSize());
return new PageInfo<>(orderMapper.selectList(query));
不需要定义mapper接口和xml，只需要在对应数据库订单表的实体类Order中添加mybatis-plus注解@TableName("user")@TableId(type = IdType.AUTO)@TableField("user_name")


Wapper缺点：
1.service层侵入，没有定义mapper接口规范，不能复用
2.sql语句编写麻烦，复杂的sql语句处理不如原始sql
总结：mybatis-plus和mybatis本身就是相容共同促进的关系，wrapper在普通场景下的还是能极大程度地方便我们编程的

```



```
Tomcat

tomcat 和 servlet：tomcat包含servlet容器，servlet是处理请求业务的接口规范，用于实现具体业务功能
tomcat 和 spring：tomcat是中间件，是运行在jvm的进程，给spring封装了url请求到java对象的映射，和路由
HttpServletRequest是什么，如何从http请求解析到的，保存在哪如何使用/获取
HttpServletResponse如何发送回客户端？
1.tomcat是web容器，也是connector将url请求封装好的java对象，具体HttpServletRequest则
是在调用链上，不同节点对原始对象进一步的封装，链路上可以随时获取到
2.获取方式：
1)Controller中增加request参数
@RequestMapping("/demo")
public void demo(HttpServletRequest request)
2)自动注入
@Resource
private HttpServletRequest request;
3)RequestContextHolder获取
RequestContextHolder.currentRequestAttributes()
3.使用场景：AOP鉴权，切入特定注解方法，invokeBefore调用RequestContextHolder获取request.header中的jwt token鉴权。By RuoYi-Cloud

tomcat 和 spring mvc：
1.HttpServletRequest对象是Tomcat提供的，Servlet是规范，Tomcat是实现规范的Servlet容器
2.SpringMVC是处理Servlet请求的应用，其中DispatcherServlet实现了Servlet接口，Tomcat负责加载和调用DispatcherServlet
3.DispatcherServlet有自己的容器（SpringMVC）容器，这个容器负责管理SpringMVC相关的bean，比如Controler和ViewResolver等
4.同时，Spring中还有其他的Bean比如Service和DAO等，这些由全局的Spring IOC容器管理，因此，Spring有两个IOC容器。

和只使用spirng(不包含spring mvc)的区别：
共同点：tomcat都负责了负责http报文的解析
1.只是使用spring，tomcat就用自己的servlet规范实现类，触发对应的代码处理逻辑
2.使用了spring mvc，那么tomcat只是解析http报文，将其转发给dispatchsetvlet，由其管理下的bean负责处理

Spring Boot中@GetMapping("/xxx")和tomcat有什么关系
1.Spring Boot它内置了Tomcat作为默认的内嵌Servlet容器，伴随Spring Boot应用启动，并作为应用的Servlet容器来处理HTTP请求。
2.@GetMapping("/xxx")注解由Spring MVC提供，代替了web.xml servlet-mapping的作用，因为Spring MVC的注解和约定优于配置的原则，简化了开发流程。（约定配置）
===============================================================
servlet只是一个java接口，定义的是一套处理网络请求的规范，其中最主要的是两个生命周期方法 init()和destroy()，还有一个处理请求的service()。所有想要处理网络请求的类，都需要回答这三个问题：
1.你初始化时要做什么
2.你销毁时要做什么
3.你接受到请求时要做什么
servlet是一个规范，实现了servlet的类并不能直接处理请求，而是通过servlet容器

servlet容器，比如tomcat，将servlet部署到容器中，不直接于客户端端口交互，而是tomcat监听了端口，请求过来后，根据url等信息，确定要将请求交给哪个servlet去处理，然后调用那个servlet的service方法，service方法返回一个response对象，tomcat再把这个response返回给客户端。

Web服务器：将一个电脑上的资源映射成为一个URL供外界访问
Servlet容器：存放Servlet对象的容器，我们如果想要将通过URL来获得计算机上的资源，总绕不开三步：1.接收请求2.处理请求3.响应请求
由于接受请求和响应请求是共性请求，不存在差异性（也就是不需要程序员根据不同的业务场景进行自定义），所以接收请求和处理请求这两个步骤就被抽取为Web服务器。
这里Tomcat就既是Web服务器，又是Servlet容器，他接收外部请求并封装成ServletRequest，并将request发送给指定的Servlet，同时又将Servelet的处理结果整理成ServletResponse返回给客户端。

实践：
1.实现Servlet接口定义一个子类src/com.xxx.MyServelt，实现接口方法
2.在web/WEB-INF/web.xml 里面注册servlet信息<servlet/><servet-mapping/>
3.定义前端index.jsp <a href="${pageContext.servletContext.context}/xxx"/>

JavaWeb的三大组件分别是Servlet/filter/Listener。我们在使用过程中，再也没有new过他们，也没有main方法去执行，只是按照接口规范写好这个三大组件对应的类，然后在web.xml中配置一下，他们就可以运行了
前后端不分离：所有请求包括前端请求，统一进入Tomcat，经过Filter分发到各自的Servlet中，比如:后端接口调用/AServelet -> Aservelet, index.jsp - > JspServlet, index.html -> defaultServlet

===============================================================
JSP
问题：请求index.html静态资源时，服务器端是我们用Java开发的，它用什么响应这一大堆html语句？
答案就是：Servlet！

早期的黑暗时刻：在java response.getWriter().println("<html>")写html片段，靠java的拼接将动态数据(对象)写入。我们想在在其他工具中写好html语句，再在其中嵌入我们的数据即可，由这个工具生成最终的html静态语句返回给浏览器。而JSP就是这个工具。

JSP：Java Server Page，直译就是“运行在服务器端的页面”。
当有人请求JSP时，服务器内部会经历一次动态资源（JSP）到静态资源（HTML）的转化，服务器会自动帮我们把JSP中的HTML片段和数据拼接成静态资源响应给浏览器
即：JSP = HTML + Java片段（各种标签本质上还是Java片段）

为什么说JSP本质上就是一个Servlet？
1.WEB容器接收到以.jsp为扩展名的URL的访问请求时，它将把该请求交给JSP引擎去处理。Tomcat中的JSP引擎就是一个Servlet程序，它负责解释和执行JSP页面。
2.Tomcat把为JSP页面创建的Servlet源文件和class类文件放置在目录中:index_jsp.java,index_jsp.class
3.相当于Tomcat帮我做了“逐行复制HTML代码到Servlet”这一步。

jsp vs ajax+html
请求、响应一来一回，就两样：数据+HTML骨架。运输方式有两种
1.服务端组装好再发货(jsp)
2.部件拆开运输，买家自己组装(ajax+html)。异步请求，先返回html页面，等待请求数据予以响应

MVC(Model/View/Controller) vs JAVAEE三层架构(Controller/Service/Dao)
MVC是web开发都有的一种模式，并非JavaEE独有的开发模式。可以理解MVC只是在web层，对底层细节做了封装隔离（M=Service+Dao）

===============================================================
Tomcat
tomcat是web服务器，将接收和响应请求的过程从请求链中分离出来，共性请求作为复用模块包含了请求url+header+body 和 Java类的双向映射
tomcat又是servlet容器，开发人员可以根据不同业务场景，分发到不同Servlet中，处理具体业务，也就是自定义实现Servlet接口
定义：Tomcat是运行在JVM中的一个进程。它定义为“中间件”，顾名思义是一个在Java项目与JVM之间的中间容器。
tomcat管理3种资源：
1.Servlet -> InvokerServlet
2.JSP -> JspServlet -> 根据url找到*.jsp -> ‘翻译成’Servlet
3.静态资源 -> DefaultServlet -> HttpServlet(doGet/doPost) -> serveResource.copy() // 把静态资源对应的输入流读取出来，扔到了输出流里

tomcat文件架构：/webapps部署web应用 /work保存jsp生成的servlet和class
/conf/web.xml：如果项目中没有配置web.xml则取用默认文件，有则覆盖。配置url请求和Servlet之间的映射规则，和一些自定义的servlet参数配置
/conf/Server.xml保存配置信息
<Server port=8005>
<Service>
n*<Connector/>
1*<Engine>m*<Host><Context/></Host></Engile>
</Service>
</Server>
Tomcat容器框架——Catalina
Connector:只负责监听不同协议的端口，比如Http/1.1，AJP/1.3等，将请求带到Engine
Engine: 引擎，管理多个虚拟主机
Host:主机端口，部署的web应用,可扩展新建虚拟主机
Context：部署的web应用，包含多个Sevlet封装器,取出请求物
Wrapper: 封装器，容器最底层，对Servlet进行封装，负责实例的创建、执行和销毁
容器的请求是在上述容器之间层层调用，每层之间都是调用链上的节点，对到达该链路的Request和 Response进行处理

Tomcat连接器框架——Coyote
1.监听网络端口，接收和响应网络请求
2.网络字节流处理。将收到的网络字节流转换成 Tomcat Request 再转成标准的 ServletRequest 给容器，同时将容器传来的 ServletResponse 转成 Tomcat Response 再转成网络字节流。


```



```
请求前端页面时：tomcat vs nginx
1.Apache或者Nginx 是HTTP Server，始终只是把服务器上的文件如实的通过 HTTP 协议传输给客户端。
2.Tomcat 则是一个Application Server，本质上它是一个Servlet/JSP应用的容器。需要Java Runtime环境和自定义Servlet处理方法的支持，提供动态资源的支持，比如占位符替换、ajax异步刷新等。也集成了 HTTP Server 的功能，应用服务器往往是运行在 HTTP Server 的背后，执行应用，将动态的内容转化为静态的内容之后，通过 HTTP Server 分发到客户端。
通常它仍然会和Nginx配合在一起使用：
1.动静态资源分离：运用Nginx的反向代理功能分发请求：所有动态资源的请求交给Tomcat，而静态资源的请求（例如图片、视频、CSS、JavaScript文件等）则直接由Nginx返回到浏览器，大大减轻Tomcat压力
2.负载均衡：当业务压力增大时，可能一个Tomcat的实例不足以处理，那么这时可以启动多个Tomcat实例进行水平扩展，而Nginx的负载均衡功能可以把请求通过算法分发到各个不同的实例进行处理

vue双向绑定 vs jsp动态替换占位符
jsp代表了传统的开发服务模式，让数据在服务端渲染完毕发送给浏览器。Vue代表着现代化的前端开发模式。
```



```
Kubernets(K8S)

```



```
ORM(Object-Relational Mapping, 对象-关系映射)
需求： JDBC 是 Java 原生的 API，支持连接并操作各种关系型数据库。JDBC太多缺点->持久层框架应运而生。
1.编码繁琐，效率低。
2.数据库连接的创建和释放比较重复，也造成了系统资源的浪费
3.大量硬编码，缺乏灵活性，不利于后期维护
4.参数的赋值和数据的封装全是手动进行
示例代码：

->Class.forName("com.mysql.cj.jdbc.Driver") 加载对应类到JVM，执行静态初始化，在这个静态初始化块中，MySQL JDBC驱动程序会向DriverManager注册自己

->Connection connection=DriverManager.getConnection("jdbc:mysql://localhost:3306/xxx", usr, pwd) 获取数据库连接对象Connection

->String sql = "select * from t_book where author = ?"; 
->PreparedStatement preparedStatement = connection.prepareStatement(sql);
->preparedStatement.setString(1, "张三");
->resultSet = preparedStatement.executeQuery();
->while(resultSet.next()){new User(rs.get(xxx))}
->xx.close()*3 & try-3*catch

MySQL8.0以上版本的数据库连接：
1.com.mysql.jdbc.Driver 更换为 com.mysql.cj.jdbc.Driver
2.jdbc:mysql://localhost:3306/数据库名更换为jdbc:mysql://localhost:3306/数据库名?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC


ORM：对JDBC进行API层的抽取和封装，以及功能的增强。会大大减少数据访问层重复性代码，比如：数据库保存、删除、读取对象信息等.例如：Hibernate 和 MyBatis
1.简单：ORM以最基本的形式建模数据。比如ORM会将MySQL的一张表映射成一个Java类（模型），表的字段就是这个类的成员变量
2.精确：ORM使所有的mysql数据表都按照统一的标准精确地映射成java类，使系统在代码层面保持准确统一 
3.易懂：ORM使数据库结构文档化。比如MySQL数据库就被ORM转换为了java程序员可以读懂的java类
2.易用：ORM封装了对持久类对象进行CRUD操作的API，例如create(), update(), save(), load(), find(), find_all(), where()等，也就是讲sql查询全部封装成了编程语言中的函数，通过函数的链式组合生成最终的SQL语句

Hibernate:全自动的 ORM 框架，程序员在使用Hibernate时几乎不用编写 sql 语句，而是通过操作对象即可完成对数据库的增删改查。

=========================================================================
MyBatis

mybatis如何连接数据库？ 通信方式是什么？ 接口和xml如何映射？ 返回数据如何映射成java类？
1.JDBC java原生API驱动。所有操作均基于该API，无论啥框架都要在databse处指定jdbc
2.指定了jdbc:mysql://hostname:port/database，就是走DNS解析，建立TCP，三次握手，发送数据包这一套了
3.mybatis-config.xml配置/spring application.yml配置，获取mapper resource，解析xml成结构化节点
4.跟IO二进制流映射java类一样，mapper.xml resultMap配置了映射信息

SqlSessionFactoryBuilder -> SqlSessionFactory
解耦合：将 SqlSessionFactory 的创建逻辑和使用逻辑分开，降低了类之间的耦合度，单一职责原则,提高了代码的可维护性和灵活性。
SqlSessionFactorys是会话工厂，职责是创建 SqlSession 实例，并负责管理这些会话对象。解耦合
Factory其实就是在封装具体实现的流程，如果你不想自己进行new+调用的话

String resource = "mybatis-config.xml";
InputStream inputStream = Resources.getResourceAsStream(resource);
->SqlSessionFactory sqlSessionFactory = new SqlSessionFactoryBuilder().build(inputStream);
-> XMLConfigBuilder parser = new XMLConfigBuilder(inputStream, environment, properties);
-> build(parser.parse());

<-------------------------------------------->
mybatis-config.xml(Mybatis全局配置文件)记录了所有mapper xml文件地址，类似于tomcat的web.xml->servlet，主启动类

为什么springboot项目中没有mybatis-config.xml？如何获取mapper
在application.yml中配置即可，spring整合了mybatis，可以不用创建
mybatis:
  mapper-locations: classpath:mapper/*.xml
<-------------------------------------------->

->解析mybatis-config.xml文件，就是把所有子标签挨个解析，映射成Node树root.evalNode("xxx")获取各层级具体的value实例化成对应java对象

->上面是解析mybatis-config.xml关于mybatis config&env的配置，同级还有具体mapper resource的配置，主要是mapper路径，获取resources/mapper/下的xml文件
<mappers>
         <mapper resource="UserMapper.xml"/>
</mappers>

->解析一个mapper.xml所有节点数据，同理上面。比如解析namespace,resultMap等等。重点是:
->context.evalNodes("select|insert|update|delete")装的是xml中的所有sql节点，比如select insert update delete ，每一个sql是一个节点。循环解析每一个sql节点。
->parseStatementNode()，解析单个sql标签里的所有数据，
->并把所有数据通过addMappedStatement这个方法封装在MappedStatement这个对象中。解析xml的目的就是把每个xml中的每个增删改查的sql标签解析成一个个MapperStatement并把解析出来的这些对象装到Configuration的Map中备用
->返回 configuration.addLoadedResource(resource);把这个解析完的mapper xml的名字装到set集合中
->bindMapperForNamespace();1)获取mapper名称空间，一般是mapper的全限定包名，它通过反射获取这个mapper的class对象。
->knownMappers.put(type, new MapperProxyFactory<>(type));维护了一个Map对象，key值是我们刚才通过反射生产的mapper的class对象，value值是通过动态代理生产的class对象的代理对象。
->到此mybatis根据主配置文件初始化就完成了。简化：配置config&env，把每个sql标签解析成mapperStatement对象装进集合，然后把mapper接口的class对象以及代理对象装进集合

总结一句话：SqlSessionFactory sqlSessionFactory = new SqlSessionFactoryBuilder().build(inputStream);

注意：上面介绍的是resource引入xml的方法，但是引入xml的方式有4种，其中单文件引入方式还有url方式和class方式，url方式就是直接引入一个xml和resource方式一模一样。而class方式是引入一个mapper接口和（resource和url方式相反）
这时候sql语句的单元是mapperStatement

<-------------------------------------------->
获取session会话对象源码分析, session是我们与数据库互动的最上层api，所有的增删改查都要调用session
->SqlSession session = sqlSessionFactory.openSession();
->解析主配置文件把所有的节点信息都保存在了configuration对象中，它开始直接获得Environment节点的信息，这个节点配置了数据库连接和事务。
->之后通过Environment创建了一个事务工厂，然后通过事务工厂实例化了一个事务对象。
->最后创建了一个执行器Executor，我们知道session是与数据库交互的顶层api，session中会维护一个Executor来负责sql生产和执行和查询缓存等

mybatis的执行器有三种:
1.SimpleExecutor: 简单执行器，是MyBatis中默认使用的执行器，每执行一次update或select，就开启一个Statement对象，用完就直接关闭
2.ReuseExecutor: 可重用执行器，这里的重用指的是重复使用Statement，它会在内部使用一个Map 把创建的 Statement 都缓存起来，每次执行 SQL 命令的时候，都会去判断是否存在基于该 SQL 的 Statement 对象，如果存在 Statement 对象并且对应的 connection 还没有关闭的情况下就继续使用之前的 Statement 对象，并将其缓存起来。
每一个SqlSession 都有一个新的 Executor 对象，所以我们缓存在 ReuseExecutor 上的Statement 作用域是同一个 SqlSession。
3.BatchExecutor: 批处理执行器，用于将多个SQL一次性输出到数据库

这时候sql语句的单元是session，session会从存储的Map中取出对应的MapperStatement对象，包含该sql所有的出参类型，出参值，入参类型，入参值还有sql语句等等。
<-------------------------------------------->
查询过程源码分析：
Object o = session.selectOne("com.wsdsg.spring.boot.analyze.mapper.UserMapper.selectUserById", 1);

->有缓存获取缓存，没缓存执行器Executor,调用底层JDBC API执行sql语句。前提是完成一系列JDBC前置准备工作，建立连接，prepareStatement
mybatis有一级缓存和二级缓存，默认二级缓存是不开启的，可以通过配置开启。而一级缓存是开启的，一级缓存是session级别的缓存，mybatis在查询的时候会根据sql的id和参数等生产一个缓存key. sql对应session。非查询操作会删除缓存

->repareStatmentHandler,替换占位符等等,return的全是prepareStatement预编译会话对象，说明mybatis默认就可以防止注入攻击
->preparedStatement.execute();得到完整的sql语句，调用jdbc接口执行sql语句，处理返回值
->resultSetHandler，结果集处理器.作用是：负责将JDBC返回的ResultSet结果集对象转换成List类型的集合

说到底就是封装了jdbc操作数据库的步骤，最终还是和jdbc操作数据库的步骤一模一样。他的封装就是为了让我们可以更方便的传参和处理结果集。
```



```
RPC（Remote Procedure Call，远程过程调用）
OpenFeign是一个声明式的HTTP客户端框架，用于在微服务架构中进行服务间的通信。虽然可以用于实现远程过程调用（RPC），但不是传统意义上的RPC框架。
RPC是一种通信模式，允许一个进程（或者一个计算机程序）调用另一个地址空间（通常是另一台机器上）的子程序，就像调用本地子程序一样。RPC框架提供了一种简单的方法来实现远程过程调用，隐藏了网络通信的细节，使得在分布式系统中进行远程调用更加方便。

在Spring生态系统中，有几种实现RPC框架的方案：Spring Remoting，Spring Cloud Netflix，Spring Cloud gRPC，Spring Cloud Dubbo
通常会涉及到多个独立部署的服务之间的通信，可能会避不开RPC

那我用openfeign来进行服务间调用，是不是可以避开rpc调用？
实际上，OpenFeign本身就是一种RPC框架，虽然它更多地关注于HTTP通信，但本质上仍然是一种RPC方式。所以，使用OpenFeign进行服务间调用本质上仍然是RPC调用。

RPC框架指的是：可以让客户端直接调用服务端方法就像调用本地方法一样简单的框架，比如我前面介绍的 Dubbo、Motan、gRPC 这些。 如果需要和 HTTP 协议打交道，解析和封装 HTTP 请求和响应。这类框架并不能算是“RPC 框架”，比如 Feign。

为什么服务间调用必须是RPC，还有别的吗，RPC对比他们好在哪？
1.RPC
优点：
直接调用：RPC调用方式更接近本地调用，调用方不需要了解底层网络通信的细节。
性能高：RPC调用通常比基于消息队列的通信方式更快速，因为它避免了消息序列化和反序列化的开销。
缺点：
耦合性高：RPC调用通常需要提前定义接口和数据结构，服务间的耦合性较高。
难以扩展：当系统规模扩大时，RPC调用可能会带来一些管理和维护上的挑战。
2.消息队列
缺点：
复杂性高：消息队列的配置和使用相对复杂，需要考虑消息序列化、消息格式、消息路由等问题。
延迟较高：由于消息队列是异步通信，所以消息的传递可能会有一定的延迟。
3.RESTful API：是一种基于HTTP协议的通信方式，通过HTTP请求和响应来进行通信。它使用HTTP的GET、POST、PUT、DELETE等方法来表示不同的操作。
缺点：
性能较差：相比于RPC调用，RESTful API的性能通常较差，因为它使用文本协议，需要进行序列化和反序列化。
约束较多：RESTful API通常需要遵循一定的规范和约束，如资源的URI命名、状态码的使用等。

========================================================

REST（Representational State Transfer）是一种架构风格，它定义了一组架构约束条件和原则，用于设计分布式系统。满足这些约束条件和原则的应用程序或设计就可以称为是 RESTful 的。
核心原则：要求所有的客户端和服务器之间的交互都使用统一的接口。这包括使用统一的资源标识符（URI）来标识资源、使用标准的HTTP方法（GET、POST、PUT、DELETE等）来操作资源、使用标准的媒体类型（如JSON、XML等）来表示资源的表示形式等。
import org.springframework.web.client.RestTemplate;
@Resource
private RestTemplate restTemplate;
String result = restTemplate.getForObject("http://nacos-hotspot-provider/test/nacos/" + id, String.class);

RESTful对于RPC性能较差具体原理：数据序列化和反序列化
RESTful在数据传输时通常使用 JSON 或 XML 等文本格式进行数据序列化和反序列化。这涉及将对象转换为文本数据，然后再从文本数据解析为对象。这种文本格式的转换会增加处理时间和资源消耗。进一步影响网络传输的数据量和网络传输的效率。
RPC 通常使用二进制格式进行数据传输，可以直接将对象序列化为二进制流进行传输，而不需要进行文本格式的转换，因此在序列化和反序列化过程中性能更高。

========================================================
Spring Cloud是一个构建分布式系统的开发工具包，它本身是一个由多个子项目组成的集合
通用功能实现——开发工具包：Spring Cloud 提供了一些通用的功能，如外部化配置、服务注册与发现、断路器、网关等。源码中会包含这些功能的实现代码，以及一些与这些功能相关的辅助类和工具类。
可拓展：预留出上述通用功能的接口和规范

SpringCloud的常用组件（子项目）
Spring Cloud Netflix：Netflix OSS 开源组件集成，包括Eureka、Hystrix、Ribbon、Feign、Zuul等核心组件。整体进入维护模式
spring cloud alibaba 包含Netflix功能，包括：Nacos，Sentinel，RocketMQ，Dubbo，Seata，Alibaba Cloud OSS，Alibaba Cloud SchedulerX，Alibaba Cloud SMS
Spring Cloud Consul：基于Hashicorp Consul的服务治理组件。
Spring Cloud Config/Spring Cloud Sleuth/Spring Cloud Stream/Spring Cloud Security
```



```
手写RPC框架：一款基于 Netty+Kyro+Zookeeper 实现的 RPC 框架。spi热插拔=动态config

RPC（Remote Procedure Call） 即远程过程调用:可以让客户端直接调用服务端方法就像调用本地方法一样简单的框架，比如我前面介绍的 Dubbo、Motan、gRPC 这些。 如果需要和 HTTP 协议打交道，解析和封装 HTTP 请求和响应。这类框架并不能算是“RPC 框架”，比如 Feign。

Netty:网络传输,既然要调用远程的方法就要发请求，请求中至少要包含你调用的类名、方法名以及相关参数吧！推荐基于 NIO 的 Netty 框架。
Kyro:序列化 ,既然涉及到网络传输就一定涉及到序列化，你不可能直接使用 JDK 自带的序列化吧！JDK 自带的序列化效率低并且有安全漏洞。 所以，你还要考虑使用哪种序列化协议，比较常用的有 hession2、kyro、protostuff。
Zookeeper:注册中心,注册中心首先是要有的，推荐使用 Zookeeper。注册中心负责服务地址的注册与查找，相当于目录服务。服务端启动的时候将服务名称及其对应的地址(ip+port)注册到注册中心，服务消费端根据服务名称找到对应的服务地址。有了服务地址之后，服务消费端就可以通过网络请求服务端了。

同是阿里的产品，为什么开发出Dubbo与Nacos看似重叠的组件（都负责服务注册发现，服务间调用）？
其实是合作的关系。Naocs对于Dubbo，就像这里ZooKeeper对于RPC。
Dubbo 是一个 RPC 框架，负责服务间的调用，而 Nacos 是一个注册中心，用来存服务的数据。两者是合作的关系，可以将 Dubbo 的服务注册到 Nacos 中，这样 Nacos 就可以作为一个集中的服务目录，为其他服务和客户端提供查询和发现的能力。此外，Nacos 的配置管理功能可以与 Dubbo 配合使用，以实现动态的服务配置。
Nacos 是 Dubbo 生态系统中重要的注册中心实现

本质:服务间通信，一切拓展是基于调用链上某一节点暴露出来的底层api(linux sokcet aip 常见基于库c++，可java,go,python)，这也是gateway在其层面上为什么屏蔽不了其他服务，只能在更高一层只暴露gateway端口。
但动态config如何实现？，看来服务暴露的api不止controller。
这也是为什么nacos需要提前启动，占据端口
一切服务都要配置nacos，要nacos未启动或找不到动态config则服务启动失败
sentinel不需要配置nacos就启动，原因是sentinel获取调用链路是基于AOP（更底层的API，只有pom+yml配置了才行=才能应用AOP），通过自定义的拦截器（通常是 Servlet Filter、Spring Interceptor 等）来切入应用程序的核心逻辑，实现对流量的控制、熔断降级等功能。

Java ：动态代理机制；序列化机制以及各种序列化框架的对比，比如 hession2、kyro、protostuff；线程池的使用；CompletableFuture 的使用
Netty ：使用 Netty 进行网络传输；ByteBuf 介绍；Netty 粘包拆包；Netty 长连接和心跳机制
Zookeeper:基本概念；数据结构；如何使用 Netflix 公司开源的 zookeeper 客户端框架 Curator 进行增删改查；

既然有 HTTP 协议，为什么还要有 RPC?
1.明文传输，序列化反序列化，网络存储传输的压力都更小
2.减少了http header重复状态位冗余
3.作为服务间调用方便，具有统一规范接口，屏蔽底层细节，方便易读易扩展

spi热插拔：
利用动态代理->反射的原理
JDK内置的工具包ServiceLoader，。也可以自定义或者封装
ServiceLoader会自动识别resources/META-INF/services/名为 com.example.MyService 的文件，内容如下
com.example.MyServiceImpl1
com.example.MyServiceImpl2

此时客户端创建ServiceLoader存有这两个实现类，热插拔的概念是客户端可以指定某一个实现类，过程中可根据方法调用或修改配置文件替换为另一个，默认是list.get(0)
指定某一个实现类的前提是已知其全限定包名，类似于Map的思想，用包名作为key去获取该实例，在ServiceLoader上面封装一层，通过对currentService的set和get，实现唯一服务替换和获取（需要是单例的）


IO通信，NIO避免为每一个连接维护一个线程，选择epoll()通过中间件（请求队列），操作系统收到新的request后会保存在tcp文件描述中，并向中间件中push一个就绪状态。多路复用，只需要单线程对该队列进行维护就性了，while检查队列不为空。

socket是TCP协议，操作系统提供了接口，在这之上就是根据服务间通信自定义衍生出来C/S B/S S/S各种协议

fd = socket(AF_INET, SOCK_STREAM, 0),套接字的创建是在传输层（Transport Layer）进行的，而不是在会话层（Session Layer）
private static Map<Socket, String> sessionMap = new HashMap<>();
// 在这个示例中，我们假设客户端与服务器建立了一个 TCP 连接
Socket clientSocket = new Socket("127.0.0.1", 8080);
// 在建立连接后，保存会话信息到会话管理中
String sessionId = "abc123";
sessionMap.put(clientSocket, sessionId);
// 通过会话管理查找特定 Socket 对应的会话信息
String sessionInfo = sessionMap.get(clientSocket);
System.out.println("Session ID for this client connection: " + sessionInfo);

public String login(HttpSession session)
session.setAttribute("username", "张三");
当客户端发送请求到服务器时，服务器会为该请求创建一个 Session（Map，字典），并为该 Session 分配一个唯一的标识符。这个标识符会被发送到客户端，并保存在客户端的 Cookie 中。当客户端发送下一次请求时，客户端会将该标识符再次发送给服务器，服务器通过该标识符来获取对应的 Session，从而获取之前保存的状态信息。
也解决了HTTP/1.1 的缺点：无状态，下单流程反复验证身份
```



```
应用层：为应用之间通信提供底层接口，暴漏服务端资源接口+参数+返回类型。可以扩展出约定单个消息长度，压缩方式，序列化于反序列，权限验证，加密方式等功能

传输层：可靠传输协议，可靠指：保证数据传输过程的数据完整性，按序接收和重发。额外：建立连接，面向二进制字节流，流量控制，拥塞控制，超时重传

网络层：负责具体的传输过程（数据从一个设备传输到另一个设备），首先就要明确发送地址（IPv4:网络号+主机号+掩码），其次就是每到一个节点前往下一节点的方向（路由寻址，前往下一个路由器）.知道对方的 IP 地址之后，接下来需要通过 ARP 协议根据 IP 地址查询 MAC 地址，并将查询的结果作为接收方 MAC 地址。

网络接口层：IP地址在以太网行不通，需要MAC地址作为头部添加上去，ARP获取。以太网是一种局域网，将附近网络设备连接起来，网络接口就是要连接的以太网接口，WIFI接口，路由器网口
在网络包传输的过程中，源 IP 和目标 IP 始终是不会变的，一直变化的是 MAC 地址，因为需要 MAC 地址在以太网内进行两个设备之间的包传输。
MAC 的作用则是实现「直连」的两个设备之间通信，而 IP 则负责在「没有直连」的两个网络之间进行通信传输。

当应用程序通过 Socket 接口发送数据包，数据包会被网络协议栈从上到下进行逐层处理后，才会被送到网卡队列中，随后由网卡将网络包发送出去。

告诉操作系统网络包已到达（触发ack，避免超时重传），中断方式：
当有网络包到达时，会通过 DMA 技术，将网络包写入到指定的内存地址，接着网卡向 CPU 发起硬件中断，当 CPU 收到硬件中断请求后，根据中断表，调用已经注册的中断处理函数。
硬件中断处理函数会做如下的事情：
1.需要先「暂时屏蔽中断」，表示已经知道内存中有数据了，告诉网卡下次再收到数据包直接写内存就可以了，不要再通知 CPU 了，这样可以提高效率，避免 CPU 不停的被中断。
2.接着，发起「软中断」，然后恢复刚才屏蔽的中断。
软中断：后台 ksoftirqd 内核线程收到软中断后，就会来轮询处理数据。

发送网络数据的时候，涉及几次内存拷贝操作？3次
1.应用间通信一次请求发送，将其内容拷贝到sk_buff内存
2.进入网络层，每一个sk_buff会创建一个副本，可靠传输。发送时，副本立即释放，收到ack再释放原始sk_buff
3.sk_buff大于MTU，sk_buff会被拷贝到多个小的sk_buff

HTTP缓存
可以是AOP redis get请求会缓存会过期，post清除缓存，但需要有外部源改动时刷新缓存

公钥加密，私钥解密：保证接收方是安全的
私钥加密，公钥解密：保证发送方是安全的。数字签名算法，防止篡改就是内容摘要(他懂这个hash，且摘要占用空间不大，主要对内容完整性校验)+数字签名（他是你要找的人）。这一步已经可以保证身份正确了，但要防止公钥+私钥整体被替换就需要一个绝对安全的第三方保存，个人是没有公信力的
```



```
通常指令的大小是4字节，也就是32位。这意味着每个指令占用4个字节的存储空间
也就是内存空间是以字节也就是8位为最小单元的

硬中断是一种异步的事件处理机制，可以提高系统的并发处理能力。由硬件(DMA)触发中断
但要求中断请求的处理程序应该要短且快，因为中断处理程序在响应中断时，可能还会「临时关闭新中断」，也就说中断有可能会丢失
因此引入软中断，延迟处理剩余部分。由内核(后台线程)触发中断

虚拟内存是为每个进程分配一套独立的虚拟地址，来实现读写隔离。具体由操作系统进行映射，保证进程间的地址是隔离了，而且映射过程对进程透明（进程只能获取到统一的命名空间，由操作系统负责实际映射，因此进程无法指定操作物理内存，也从值域上接触不到其他进程的物理地址。相当于了统一了外部接口和约束，由具体业务负责map分发）
因此虚拟内存是为进程设计的
操作系统会提供一种机制，将不同进程的虚拟地址和不同内存的物理地址映射起来。
但只是每个进程的用户态内存隔离，内核态是共享的（也就是即使内核态地址不同，实际指向的都是同一块物理内存）

以页为单位必然解决了外部碎片的问题，即使出现中间的空闲页，程序又不要求空间必须连续，就可以先填中间空闲页再接着填队尾的空闲页
内部碎片，是因为页是分配的最小单位，如果程序本身小于页空间就会有内部碎片，但又不能填入其他程序，因为虚拟内存是要保证隔离而页又是最小单位，因此不同程序的页号必然不同。这时如果页面越小，碎片必然越来越小，但会降低IO速度，需要多次取出页面数据才能完成之前的一次页面读取
且基于页的换入换出必然比段快，本质是页普遍比段小，且不需要整体置换。换入换出发生在内存不够的情况（或者节约/限定空间不一次性加载全部），根据lru进行淘汰，

多级页表：
假设有10个页表，我告诉进程我有10个，需要map保存10对应的页表头，要不然他不知道是哪10个（因为页表之间不一定连续，就不能用基准+偏移+固定大小，来划分），这时候我要用第6个，map就get第6个。我用多级页表，分成2*5，我只需要map保存2个对应页表头，他们有责任保证我能找到前5个或后5个中的任一个，我要用第6个，就去map中根据第二个表头信息，创建第6页的页表头，也就保存了3个表头，时间换空间的体现。
<发现虚拟内存对应的页没有在物理内存中，就会触发缺页中断，进程会从用户态切换到内核态，然后操作系统会建立虚拟内存和物理内存之间的映射关系><还可以延申出空闲空间不够，内存回收的知识>
这时候如果占满肯定是多级页表比正常多了许多索引表，占用空间是大了。但还是优于正常的？
1.但这时候肯定整体都很庞大了，多这一两个忽略不计（索引的大小是常数的，不受数据数量级影响）。
2.而且一般是换入换出实际用不到这么多，换入换出就是时间换空间了，对时间要求高就不换了接着开辟新空间。相当于为进程分配虚拟内存时告诉进程时间性能的极限我给你准备好了，用不用看你自己

缓存cache淘汰策略：预读失效和缓存污染
预读失效是基于一次查询，查询到无用的之后一直没有用到
缓存污染是失效预读把热点给淘汰了（或者是普通的把热点淘汰了）
Redis LFU 针对热点的缓存污染问题
Mysql和OS是改进LRU基于上诉两个问题
老年代和新时代，active-inactive
第一次先加入老年代，间隔一定时间（验证缓存健壮性，立即重读的不一定是）再次读取加入新生代，要不然逐渐移到队尾（无用内容停留时间尽可能短）

操作系统总是积极地将所有空闲内存都用作 Page Cache 和 buffer cache，当内存不够用时也会用 LRU 等算法淘汰缓存页。

PageCache = 内核空间 为用户空间共享提供缓存
零拷贝技术是基于 PageCache 的，PageCache 会缓存最近访问的数据
传输大文件时，不能使用零拷贝，因为可能由于 PageCache 被大文件占据，而导致「热点」小文件无法利用到 PageCache，并且大文件的缓存命中率不高，这时就需要使用「异步 IO + 直接 IO 」的方式。

如何优雅的解决了并发插入的问题？
插入意向锁之间互不排斥(是一种特殊的间隙锁，间隙锁之间是兼容的)，但会被不同事物间隙锁阻塞，所以即使多个事务在同一区间插入多条记录，只要记录本身（主键、唯一索引）不冲突，那么事务之间就不会出现冲突等待。

insert只会加插入意向锁，update和delete会加X型行级锁。普通select不加锁，靠mvcc控制事务隔离。in share mode / for update 会加S和X锁

加锁的对象是索引，加锁的基本单位是 next-key lock，只是可能退化成记录锁和间隙锁

记录锁是加在具体记录上的，不能误解成查询条件的等值都被锁上了，所以只适用于唯一索引。间隙锁才能锁住该等值范围。判断next-key是否退化时，可以考虑只加记录锁管不管用。

进行范围查询时，会对每一个扫描到的索引加 next-key 锁

insert 加的是隐式锁，只有在触发一定条件才加锁
1.已有间隙锁，插入意向锁，释放间隙锁后插入
2.唯一键冲突，本身插入插座返回失败，还要加上S型记录锁，防止其他事务对该重复值进行update/delete，造成不一致（有点像不可重复读一样）

死锁检测：监测程序主动回滚某一事务，破坏环路条件
超时等待：自行回滚该事务

死锁场景：间隙不冲突，间隙与插入冲突

执行一条 update 语句，期间发生了什么？
查询语句的那一套流程，更新语句也是同样会走一遍。不过，更新语句的流程会涉及到 undo log（回滚日志）、redo log（重做日志） 、binlog （归档日志）这三种日志

MySQL 会隐式开启事务来执行“增删改”语句的，执行完就自动提交事务的

undo log 和数据页的刷盘策略是一样的，都需要通过 redo log 保证持久化。

buffer pool存缓存页，包括各种类型：数据页-脏页，索引页，undo页。所有的对页的修改都会记录到redo log
WAL（Write-Ahead Logging）技术= 完成就立即写入日志 + 再适时写回 = 写操作-写入磁盘两次

事务提交时，只要先将 redo log 持久化到磁盘即可。事务提交之前发生了崩溃，重启后会通过 undo log 回滚事务，事务提交之后发生了崩溃，重启后会通过 redo log 恢复事务

写入 redo log 的方式使用了追加操作， 所以磁盘操作是顺序写，而写入数据需要先找到写入位置，然后才写到磁盘，所以磁盘操作是随机写。虽然写两次，但redo log开销更小

redo多一步保险，要求性能上有优势。和数据页面向的功能不一样决定的，就是专门干这个的

binlog在redolog存的是差不多的东西都是存修改，不过binglog可以直接存值
binglog在redolog前面，myisam不具备crash-safe才引入的redolog，推测是redolog是循环写，binglog是全量，同步的速度要快，占用空间少->IO快，恢复快。（其实binlog也可以用来恢复，不过时间线更前面——刷盘策略能看出，但如果整个数据库被删除redo是恢复不了的，只能用binlog）

主从复制的原理是：通过中间件logfile记录发生的更改，虽然存在时间差/速度差，但由于是logfile不为空就同步，不一定时刻都有新的修改，所有总能追上，只是压力大时速度不匹配问题

主从复制之后，你就可以在写数据时只写主库，在读数据时只读从库，这样即使写请求会锁表或者锁记录，也不会影响读请求的执行。

redolog执行完就吃可以被持久化了，binglog要事务提交前。保证搭配使用redolog能判断回滚当前事务

组合优于继承：使用继承保证父类能够出现的地方，子类一定能够出现（反例：鸟.fly() 但鸵鸟就不行，要重写fly方法抛出异常，但如果是实现fly()接口就比较灵活了，再封装成FlyAbility类，就是组合+委托，组合可以是fly()接口+layEgg()接口，共同委托给一个无继承的类，但缺点是层级关系不明显/维护成本高）

总结RPC实现+简历

AQS是你想对等待线程queue进行一些操作比如semaphore多个进入，countdownlatch/clclicbarrier是等多个起出
synchronized，reentrantlock，stampedlock读写锁是对单个线程的操作，其他就老老实实的等待
CLH 锁是对自旋锁的一种改良，是一种隐式的链表队列

ThreadLocal场景指伴随线程可以随时存取的context，不受方法调用参数传递的约束，确保唯一的且线程安全，场景用于追踪链路id，鉴权过滤链中保存用户权限信息。 spring security中SecurityContextHolder.getContext().setAuthentication(authenticationToken)

CompletableFuture异步任务编排，正常多线程之间并行控制先后顺序只能通过锁来实现，但会使代码结构复杂混乱，而且多对1更麻烦，异步编排可以解决这种依赖关系，之前都是单一锁资源依赖而且控制不了start顺序，现在相当于请求前置的n个锁才执行，前n个锁互不干扰
```

```
矩阵A的作用就是状态转移，捕获迄今为止看到的每个token之间的区别。因此可以自定义更新公式，不用按照深度学习的参数来按梯度更新

用Convolutional表示进行快速的训练
状态转移公式可以推导出ht只关于x0...xt的多项式，不需要等待前一个的状态，因此可以并行计算

为什么需要离散数据的连续化/为什么需要零阶保持技术
离散->连续->采样：连续才可导，embdding层负责将id转换成d维向量，中间各种层的处理本质是分布变换，最后在最终分布上采样得到预测label
连续数据：文本数据在深度学习中被当作具有序列特征的连续数据来处理
离散数据连续化：指的是从集合到序列的过程，也可以是从id->hidden的变换
音频/视频这种无限上下文长度的数据，信息熵较低，就需要被按间隔时间步t采样，有限间隔的上下文足够了

有空真想再学一遍离散数学/概率论/线代，机器学习+深度学习基础
PPT
1.离散连续之间的变换
2.序列信息长期记忆的本质
3.RNN<->transformer/cnn 获取序列信息的方式
4.优缺点+改进

长期记忆的关键
金融例子EMA，不需要像transformer一样汲取无限长上下文(unbounded context)来获取结果。通过summary历史信号的加权平均一样可以x=(1-a)x+au（用函数曲线来保存pattern理论上比单纯从context中提取pattern更具鲁棒性，但粒度差一点，本质上是历史信息压缩过程），这样做不受上下文窗口大小的影响还降低了复杂度。
但问题又来了，保存历史状态的向量就像水桶一样受固定维度的限制，满了就会遗忘太老的知识。RNN这种通过指数增长累计历史信息，会很快稀释掉历史信息，需要介入额外时间步t来限制权重稀释速度（w^t -> tw），也就是引入hippo矩阵控制增长
压缩历史信息的过程，就是对历史输入的每个向量进行压缩，想用低代价记住他就得尽量保存原始向量数组，具体就是用多项式系数作为额外参数来拟合该向量，前期由于记忆复杂度不高可以无损拟合（线代里面的Ax'=0,x'=concat(x,y)，A的秩大于x就有无穷解，x随着数量变多逐渐升维到原本的维度空间<->几何<->|A|=0有唯一解，再增大x的行数会导致求不出准确解，只能取近似解<->几何空间中就是削峰平滑化），越往后越会遗忘细节/只关注最近时间步[t-100,t)<->相当于几何中砍掉过去的x轴内容

不同维度测试模型记忆能力的下游任务
从context压缩角度来看，2/3任务解释了LTI（线性时间不变）模型失败的原因，没有针对input序列选择和遗忘的能力
1.完整复制
2.选择复制
3.总结上下文的能力

优化之后虽然可以并行，但仔细推演下流程，还是不如之前卷积并行那么简单高效，因为其相当于用冗余计算换取了并行的可行性，每一步都要重复上一步计算计算隐藏状态的过程，因此相邻时间步之间存在时延（并不像transformer所有token几乎是同时输出的，真正的互不干扰），而且时延会随着时间步增多逐渐增大并累积，第一步和最后一步存在较大的时延，但怎么说都是比串行快的

SSM+Selection
1.把原有的基于初始化的矩阵参数A,B,C，改为将几个参数∆，B, C作为输入函数s(x)的输出，实际参数还是(D,N)不过是在x上的生成的，实现了每个x都有不同的参数矩阵，而且是定制的。自此，SSN从time-vinvariant变成了time-varying
2.variable spacing
3.context filter

Mamba应用，为什么选择mamba

稀疏性的概念采用了条件计算的思想。在传统的稠密模型中，所有的参数都会对所有输入数据进行处理。相比之下，稀疏性允许我们仅针对整个系统的某些特定部分执行计算。这意味着并非所有参数都会在处理每个输入时被激活或使用，而是根据输入的特定特征或需求，只有部分参数集合被调用和运行。
```

```
didi出租车轨迹系统
系统功能：
统计车辆信息->查询车辆订单/轨迹详情->分析用户行为数据
查询实时全局车辆位置，和给定时间段内所有行驶车辆的移动轨迹动画，根据车辆道路通过速度可视化道路拥堵情况
基于网格统计用户不同时刻上下车位置，计数，可视化，用网格分级展示上下车热点区域

加分项：
模拟数据实时导入，低延迟快速可视化
并发查询速度优化，QPS
上下车位置分析用户偏好，统计分析数据
路线优化/拥堵避免/推荐车辆漫游路线



城市兴趣点POI系统
1.数据爬取：美团，大众点评，百度/高德地图
2.数据处理

python3 -u train.py --gpu 7 --dataset 7thousand --hidden-dim 32  --validate-epoch 50 --epochs 50 --rnn lstm --save 7thousand_mamba_hid32

找到一个故事（两个）->设计符合pattern的数据->比较
Mamba 1.选择记忆能力 2.efficienct的能力，但前提是上下文够长

最需要的是找故事
其次是参考mamba应用，改进着用
1.mamba+transformer
2.扩展序列长度，加入更复杂的pattern。local+encoder,global+mamba,不同时间轴上提取子序列

one-long-tra应该一个用户一轮epoch只训练一次，包含该用户所有的历史轨迹

在我们数据集上，正常mamba block略优于同等参数设置的lstm和transformer，增加mamba层数或维度并无提升。
最下面一行最优，是考虑到mamba优势在长序列处理上，准备调整一下序列长度看下，看下mamba性能是否随序列增长单调递增。就在训练/推断时把当前用户当前位置的所有历史序列作为mamba输入序列，也就是只预测最后位置的下一位置。并且同等设置下还优于lstm和transformer，证明mamba确实最优，关键是突破了之前在该数据集上所有模型的上限，mrr增长了2-3个点，不过运行速度有些慢代码上可以优化一下
序列增长有用，再考虑在时间或空间维度扩充序列长度，比如拼接最近、日周期、周周期的时间子序列，或者空间上拼接相似度高的子序列

把这三篇文章好好看了，找老师讨论下开题的事（明天下午讨论，明天上午准备下文档+PPT，应该是讨论工作顺带的），，系统+轨迹预测+重分析

回顾之前看过的三篇关于历史序列扩充的论文，寻找一个有效的方法能使mamba效果随序列长度增长进一步提升（因为之前用到了全部历史序列已经到头了，寻求在新的时间/空间维度上继续扩充序列，或则沿着之前关于global location/qt的路子尝试能否基于mamba改进）
《Where to Go Next: Modeling Long- and Short-Term User Preferences for Point-of-Interest Recommendation》
《Trajectory WaveNet: A Trajectory-Based Model for Traffic Forecasting》
《Graph Mamba: Towards Learning on Graphs with State Space Models》

近十年来，下一个位置预测问题得到了广泛的研究，主要分为两大类:传统的数学方法和深度学习方法。传统方法主要利用数学预测模型。例如，方法包括从用户位置矩阵分解中获得推荐[17]，使用马尔可夫链(MC)构建位置转移概率矩阵[12]，或将两者集成以开发个性化的MC[25]。贝叶斯理论也被用于预测下一个位置[4]。

相反，深度学习方法通过增强它们的kdd来解决传统方法在顺序转换建模中的缺点。

识别时间和空间依赖性的能力。基本框架包括卷积神经网络(cnn)[5,41]、rnn[21,24,35]和变压器[1,33,36]。

例如，CEM[5]通过使用一维卷积模拟位置的相对顺序来预测下一个位置。

Flashback[35]为RNN模型设计了一种特殊的隐藏状态聚合机制来预测下一个位置。GETNext[36]通过合并一个全局轨迹流映射来对公共顺序转换信息建模，从而增强了Transformer。

研究背景和模块设计上写的都不太行

用户轨迹数据导入与维护模块，基于交通摄像头数据的轨迹分析模块，基于出租车GPS数据的轨迹分析模块，基于出租车乘客上下车位置的城市热点分析模块，基于交通摄像头的城市流量与流向分析模块，基于用户历史轨迹的轨迹预测模块
```

```
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        char[] arr = sc.next().toCharArray();
        int n = arr.length;
        int res = 0;
        for(int l=0;l<n;l++){
            int cnt0 = 0;
            int cnt1 = 0;
            for(int r=l;r<n;r++){
                if(arr[r] == '0') cnt0++;
                else cnt1++;
                if(cnt0>cnt1) res++;
                else break;
            }
        }
        System.out.println(res);
    }
```

